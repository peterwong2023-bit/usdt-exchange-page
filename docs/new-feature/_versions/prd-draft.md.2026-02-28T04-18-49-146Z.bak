# 币价模板（Market）细化规格 v1.0（严格尺寸）

## 1. 模板定位（这张便签解决什么）
- 核心价值：桌面“一眼看”当前价与涨跌；需要时用规则触发变色/角标；失败可见（不上线“静默停更”）。
- 约束：严格尺寸（不可拖拽缩放），仅允许 S/M/L/XL 四档切换；默认不发系统通知/不播声音。

## 2. 尺寸档位与布局（S/M/L/XL 4 套，必须画死）
尺寸：S 240×160；M 320×220（默认）；L 420×300；XL 560×380  
通用：Header 32、Footer 24 固定；Body 自适应；Footer 永不被挤没。

### 2.1 Header（所有尺寸一致）
- 左：资产图标（币种）+ 标题（默认“BTC/USDT”或“BTC/USD”，可改名）
- 右：刷新按钮 + 齿轮（设置）+ “…”（更多菜单）
- 更多菜单至少包含：尺寸（S/M/L/XL）、置顶、锁定位置、透明度、复制、关闭

### 2.2 Footer（所有尺寸一致，强制）
- 左：最后更新（例：“2 分钟前”）
- 中：状态 Chip（正常/断网/限流/Key 缺失/Key 失效/已暂停）
- 右：动作按钮（按状态变化）
  - 正常：可不显示按钮或显示“查看告警”(如有未查看)
  - 断网/限流：重试 / 查看详情
  - Key 缺失/失效：修复 Key
  - 已暂停：恢复

### 2.3 Body（按尺寸显示的信息档位）
#### S（240×160）= “1 主 + 1 副”
- 主指标：现价（大字）
- 副指标：24h 涨跌幅（%）+ 红绿
- 不显示：高低点、成交量、图表、长文本原因

#### M（320×220）= “补 1-2 个关键字段”
- 主指标：现价（大字）
- 副指标：24h 涨跌额 + 24h 涨跌幅（同一行，红绿）
- 辅助：标的/计价（如 BTC/USDT）+ 小号“来源/交易所”(可选一行)

#### L（420×300）= “加一段趋势/扩展信息”
- 主指标：现价（大字）
- 副指标：24h 涨跌额 + 涨跌幅 + 当日开盘价（可选其一）
- 辅助：24h 高/低（两列）
- 可选（若不影响性能）：迷你趋势线（Sparkline，取最近 N 个点的本地缓存；不做交互）

#### XL（560×380）= “信息增强但不新增复杂交互”
- L 的全部
- 额外增强（二选一，v1.0 建议只做其一）：
  - A：更宽的趋势线 + 标注当前点
  - B：增加“更新时间/数据源”更完整展示（仍不做多周期切换按钮）

## 3. 数据字段定义（内部数据模型）
MarketQuote（最小结构）
- symbol：BTC
- quote：USDT（或 USD/CNY）
- price：当前价（number）
- change24hAbs：24h 涨跌额（number）
- change24hPct：24h 涨跌幅（number，-0.052 = -5.2%）
- high24h / low24h（L/XL 用，optional）
- volume24h（optional，v1.0 可不展示）
- dataTimestamp：数据时间戳（provider 返回）
- fetchTimestamp：拉取时间戳（本地）
- providerId：数据源标识
- currencySuffix：单位后缀（可配置，如“USDT”“$”“￥”）

数值格式规则（必须统一）
- price：
  - ≥1000：保留 2 位小数
  - 1~999.99：保留 2~4 位（按币种小数精度上限配置，默认 2）
  - <1：最多 6 位小数（截断不四舍五入可选；建议四舍五入）
- change24hPct：统一显示到 2 位（例：-5.23%）
- 红绿：涨=绿，跌=红；0 为中性灰

## 4. 设置项（Popover：Market 专属 + 通用）
### 4.1 概览
- 当前标的：BTC/USDT
- Provider：Binance/OKX/聚合源（示例）
- 最近成功时间：展示 dataTimestamp 与 fetchTimestamp

### 4.2 刷新
- 刷新档位：1/5/15/60 分钟
- 可见优先：默认开
- 后台降频：默认开
-（只读）失败退避说明：指数退避

### 4.3 规则（v1.0 必须可用，轻量）
规则类型（至少两种）
1) 价格阈值：price > X 或 < X
2) 涨跌幅阈值：abs(change24hPct) > X%
动作（v1.0）
- 变色（背景/边框）+ 角标
- 写入告警中心（含快照）
去重
- dedupeWindow 默认 10 分钟，可改（5/10/30/60）

### 4.4 通知通道（默认全关）
- 系统通知：Off
- 声音：Off（静音默认）
- 说明：仅开启后且未被去重时发送

### 4.5 数据源 & Key（你选“用户自备 Key”，这里必须清晰）
- Provider 下拉选择
- 该便签绑定的 Key（选择/默认 Key）
- Key 状态：有效/缺失/失效/配额不足
- 测试连接（试请求）
- 修复 Key（跳 Key 向导）

## 5. 交互流程（必须在原型里点通）
### 5.1 新建币价便签（2 步）
- 菜单栏“+”→ 选择“币价”→ 输入/搜索 BTC → 选择计价货币（默认 USDT）→ 创建

### 5.2 Key 缺失/失效修复闭环（P0）
- 便签出现，Footer=Key 缺失/失效 → 点“修复 Key”→ Key 向导粘贴 → 试请求成功 → 自动刷新 → Footer=正常

### 5.3 严格尺寸切换
- 通过“…”菜单：尺寸 → S/M/L/XL（打勾）
- 切换锚点：以窗口左上角或中心点（研发实现需统一，不允许乱跳）
- resizable=false（禁止拖拽缩放把布局搞碎）

## 6. 失败态与文案（必须统一、可理解）
- 断网（Offline）
  - Body：显示上次成功快照（价格与涨跌仍显示），并在副文本提示“网络不可用，展示的是 X 分钟前数据”
  - Footer：状态=断网；动作=重试
- 限流（429/RateLimit）
  - Body：显示上次快照；提示“请求过于频繁，已自动放慢刷新”
  - Footer：状态=限流；动作=查看详情/切换 Provider
- Key 缺失
  - Body：显示占位（—），提示“需要 API Key 才能拉取数据”
  - Footer：状态=Key 缺失；动作=修复 Key
- Key 失效（401）
  - Body：显示上次快照（若有）+ 提示“Key 无效或已过期”
  - Footer：状态=Key 失效；动作=修复 Key

## 7. 性能与缓存（给研发的硬口径）
- 通过全局调度器合并请求：同 providerId + symbol+quote + 相同刷新档位合并为 1 次请求
- Sparkline（若做）只读取本地最近 N 个点缓存；不可为图表额外引入高频接口
- 不可见降载：L/XL 的趋势线在不可见时暂停重绘（避免 CPU 热）

## 8. 初步验收标准（币价模板专属）
1) 新建成功：2 步内创建，网络正常且 Key 正确时 10 秒内出首屏数据  
2) 严格尺寸：不可拖拽缩放；仅通过菜单切 S/M/L/XL，切换不乱跳且持久化  
3) 失败态：断网/限流/Key 缺失/Key 失效均有明确中文提示 + 修复入口；断网不空白，必须显示上次快照  
4) 规则触发：阈值触发后便签变色+角标；告警中心出现记录，10 分钟内重复触发合并计数  
5) 单位后缀：支持自定义后缀/计价（至少 USDT/USD/CNY），显示一致不串单位

下一步给你两件要选（会影响原型与开发复杂度）：
- L/XL 的趋势线 v1.0 做不做？建议 v1.0 先不做或只做极简 sparkline（无交互）。
- 红绿变色清除策略：条件解除自动恢复（推荐）还是需要手动清除。