# AI 桌面便签 (AI Sticky) - PRD（定稿）

## 需求背景
当前桌面便签产品多为静态文本容器，缺乏与实时数据的交互能力。用户需要一种低打扰、高密度、自动化且具备 AI 意图理解能力的桌面挂件，用于实时监控投资行情、出行路况、环境天气及碎片化办公信息。本产品旨在通过“原子化数据源 + 轻量规则引擎 + 定向 AI 解释”的模式，打造一个常驻桌面的智能信息中心。

## 目标用户画像
1. **硬核投资者**：需实时盯盘（币/股），关注波动触发原因。
2. **高效通勤族**：关注实时路况、ETA 变化及天气突变。
3. **碎片化记录者**：需要随手记，并期望 AI 能自动整理杂乱信息。
4. **极客用户**：追求高度自定义的桌面仪表盘。

## 功能清单

### P0 - 必须有（MVP 核心）
| 功能 | 描述 | 验收标准 |
| :--- | :--- | :--- |
| **多窗口便签引擎** | 支持创建多个独立透明窗口，自由拖拽、缩放、置顶/吸附。 | 窗口状态重启后自动恢复，支持动态改变背景色和透明度。 |
| **基础数据驱动** | 支持币价（交易所 API）、股票、地理位置（IP/GPS）、天气预报。 | 数据刷新间隔可调（最低 1min），支持手动刷新触发。 |
| **记事与 AI 整理** | 纯文本便签，支持 AI 一键润色、格式化（Markdown/清单）。 | 点击“整理”按钮，3秒内输出结构化内容，保留原文。 |
| **阈值告警规则** | 简单的规则配置：如跌幅 > 5% 变红，ETA > 60min 变红。 | 触发时便签边框或背景色改变，悬浮显示触发原因。 |
| **本地安全存储** | 所有配置、Token、笔记内容存储于本地。 | 卸载或重启不丢失数据，API Key 加密存储。 |

### P1 - 应该有（v1.1 迭代）
| 功能 | 描述 | 验收标准 |
| :--- | :--- | :--- |
| **对话式解释 (RAG)** | AI 仅基于当前便签捕获的数据（新闻/行情）回答问题。 | 拒绝回答与便签内容无关的幻觉信息，标注引用数据时间戳。 |
| **自然语言生成便签** | 用户输入“帮我盯一下特斯拉，跌了告诉我”，自动组装便签。 | 准确识别“对象、规则、动作”并展示配置预览供确认。 |
| **全局性能调度器** | 针对多便签环境合并 API 请求，节省流量和电量。 | 重复数据源（如同币种）仅发起一次网络请求，后台窗口降频。 |

### P2 - 锦上添花
| 功能 | 描述 | 验收标准 |
| :--- | :--- | :--- |
| **便签联动** | 币价暴跌触发新闻便签自动搜索相关利空消息。 | 跨便签消息总线机制稳定，不产生循环触发。 |
| **iCloud 同步** | 跨设备（多台 Mac）同步便签布局与配置。 | 同步延迟小于 5 秒，解决冲突逻辑清晰。 |

## 用户故事

### 作为普通用户...
- 我可以从模板库快速拖出一个“BTC 监控位”，只需选好交易对，就能在桌角看到实时涨跌。
- 当我从 Wi-Fi 切换到手机热点，便签能感知并自动降低刷新频率，帮我省流量，老驴觉得这才是务实。

### 作为运营人员/办公族...
- 我在开会时随手在 E 便签贴入一堆乱序文字，会后点一下“整理”，它能帮我分出“待办、结论、时间地点”。

### 作为投资交易者...
- 当我关注的股票跌破止损点，便签变红并弹出 AI 简报，告诉我这是行业板块普跌还是个股利空，不用我再去翻新闻。

## MVP 范围
- **只做**：macOS 客户端 (dmg)；币价、股票、天气、交通、记事五种基础模版；基于规则的变色提醒；本地 AI 文本整理。
- **不做**：移动端同步（v1.0 暂不考虑）；全自动 Trading Bot（风险太高）；复杂的多级 Agent 协作。

## 验收标准
1. **性能指标**：常驻 10 个便签时，CPU 占用率需低于 2%，内存占用低于 200MB。
2. **稳定性**：断网环境下显示“最后更新于 X 分钟前”，恢复网络后自动重试，不弹报错弹窗。
3. **准确性**：AI 整理后的待办事项不得丢失原有的关键时间节点和数值。

## 简化级联分析
- **洞察**：所有的便签（币价、交通、天气）本质上都是“结构化数据映射到视图”。
- **简化方案**：老驴建议不要为每种数据源写一套 UI 逻辑。建立一套**通用卡片 DSL**：定义 `Header` (标题/图标), `Body` (数值/趋势图), `Footer` (更新时间/AI状态)。
- **收益**：删掉 5 类专用组件代码，新出一个“油价便签”或“粉丝数便签”只需配置 JSON，工程成本降低 70%。

## PM 辩论记录
| 议题 | PM-Alpha 观点 | 老驴观点 | 最终决策 | 理由 |
| :--- | :--- | :--- | :--- | :--- |
| **自然语言创建便签** | 核心卖点，极致体验。 | 稳一稳，识别率不稳定会导致用户配置错，风险大。 | **P1 实现** | 优先保证手动配置好用，自然语言作为快捷入口而非唯一路径。 |
| **数据源获取** | 官方提供代理服务器统一拉取。 | 成本爆炸且有封禁风险，让用户自己填 API Key 或本地直连。 | **本地化直连** | 降低运营成本，满足隐私敏感用户需求，ROI 更高。 |
| **AI 自动对话** | 每张便签都是一个 Agent。 | 概念太虚。用户只是想看为什么跌，不是想跟便签谈恋爱。 | **便签内快照问答** | 聚焦于对当前数据的解释，减少 Token 浪费。 |

## 保留的分歧
## 分歧点：数据获取成本与易用性
选项 A：用户输入自己的 API Key（如 OpenAI, Google Maps） — 优化：成本与合规 — 适用场景：极客用户、重度使用。
选项 B：由应用方代付/内置额度 — 优化：上手体验 — 适用场景：小白用户、轻量试用。
保留策略：**并行实现**。基础模版内置少量免费额度（限次），高级自定义需用户自备 Key。
触发解决条件：若 v1.0 运营成本超过营收，则强制改为选项 A。