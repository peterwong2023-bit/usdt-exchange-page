## 1) 尺寸档位（统一）
- S：240×160  
- M：320×220（默认）  
- L：420×300  
- XL：560×380  
通用：Header/Footer 固定高（如 32/24），Body 自适应；任何尺寸 Footer 不可被挤没。

## 2) 哪些模板“严格尺寸”，哪些“自由缩放”
### 2.1 严格尺寸（不可拖拽改大小）
- 币价 Market（默认 M，最小 S）
- 股票 Stock（默认 M，最小 S）
- 交通 Traffic（默认 L，最小 M）
- 天气 Weather（默认 M，最小 S）
- 日历/会议倒计时 Calendar（默认 S 或 M，最小 S）
- 番茄钟 Pomodoro（默认 S，最小 S）
- 纪念日 Anniversary（默认 S，最小 S）
- RSS/大宗外汇（后续）同上：默认 M，最小 S

### 2.2 自由缩放（允许任意尺寸，仍有最小值）
- 记事 Note（默认 L，最小 M）
- 会议记录（建议作为 Note 的一个“类型/模板变体”）：默认 L，最小 M  
  备注：自由缩放仍建议保留“吸附到 S/M/L/XL”的手感，但不是强制（可选）。

## 3) 严格尺寸的交互怎么做（用户仍要能切尺寸）
严格尺寸不等于不能换大小，只是**不能随手拉**。建议三种入口，至少做前两种：

1) 便签右上角「…」菜单增加：尺寸 → S / M / L / XL（当前打勾）  
2) 设置 Popover（概览或刷新页）增加：尺寸档位选择（同上）  
3)（可选）双击 Header：按 S→M→L→XL 循环切换

实现要点（给研发不扯皮）
- 严格尺寸模板：窗口本身 `resizable=false`，只通过“切档位”调整 frame
- 切档位时：以当前窗口左上角为锚点（或中心点）调整尺寸，避免窗口乱跳
- 尺寸变更要持久化（重启/休眠恢复）

## 4) “尺寸档位 = 信息档位”的具体约束（严格尺寸模板更需要）
严格尺寸模板只需要做 4 套布局（S/M/L/XL），避免无限适配成本：
- S：只允许“1 主指标 + 1 副指标”
- M：补 1-2 个关键字段
- L：加趋势/列表的一小段
- XL：仅做信息增强，不引入新交互复杂度（v1.0 先别塞太多）

建议默认档位（再强调一次，避免设计误判）
- Traffic 默认 L（不然拥堵条/路线会挤爆）
- Calendar/Pomodoro/Anniversary 默认 S（核心是一眼看）
- Note/会议记录默认 L（编辑舒适）

## 5) 对原型的明确要求（下一轮评审能看出“严格 vs 自由”）
- 严格尺寸模板：原型里**不要画 resize handle**；在「…」菜单里能点选 S/M/L/XL 跳转到对应尺寸 Frame
- Note/会议记录：原型保留 resize handle，并提供 Small/Large 两个 Frame 演示自由缩放效果（最小 M）

下一步你指定先从哪个模板开始细化（建议从 Traffic 或 Calendar 开始），我把对应的 S/M/L/XL 布局字段、截断规则、空态/失败态文案写成可直接给设计和研发的规格。