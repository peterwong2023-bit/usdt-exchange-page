## 1) 当前项目里最大的问题：有两份 PRD 口径在打架
- prd-draft.md：写死了“列表纯数值、S/M Gate、默认 5m、同窗切详情、退出停 OHLC、失败快照置灰”
- prd.md：还保留了 **P1：MA 均线、列表排序** 等口径，且结构与 draft 不一致

原型师只要拿错文件，就会画出“带 MA/排序/小窗画图”的版本，必然跑偏。

[便签：风险提醒] 当前存在双 PRD（prd.md 与 prd-draft.md）口径冲突，原型易选错作为唯一真相；必须合并并明确“以 prd.md 为准”。

## 2) 需要你确认：你要的“正确口径”到底是哪套（用来定唯一真相）
按你刚刚的需求描述，你要的是这套（请你确认/否决）：
- 多币种列表（Watchlist）
- 点击币种进入同窗详情看 K 线
- 小尺寸不能看 K 线：弹 Gate，引导切到 L/XL（并自动进入该币详情）
- S/M 列表**纯数值**（不画迷你趋势）
- 默认周期 **5m**
- v1.0 **不做指标**（MA/RSI/MACD 均不做）
- 退出详情页必须停止 OHLC 拉取
- 失败态必须“上次快照置灰 + 更新时间 + 修复入口”

如果这套没问题，下一步就是把 prd.md 增量修订成与 prd-draft 完全一致，并把冲突内容标记为【废弃】。

## 3) 我建议的修复动作（一次到位，避免原型继续跑偏）
1) 以 prd.md 作为“唯一对外定稿”，新增版本段落（不覆写历史）  
2) 在 prd.md 里对以下冲突点做增量处理：
- 【废弃】P1：基础均线 MA（与“v1.0 不做指标”冲突）
- 【废弃/后移】列表排序（与“v1.0 不做排序”冲突）
- 【更新】K 线默认周期从可能的 15m/1h 等口径统一为 5m
- 【更新】S/M Gate 强制、L/XL 同窗进详情、退出停 OHLC、失败快照置灰规则写死

你给一句话确认：**“以你上面那套口径为准，统一 prd.md”** 或指出哪一条要改（例如：只允许 XL 看 K 线 / S/M 要画迷你趋势 / 要做 MA），我就输出一份“增量迭代版 prd.md 完整全文（带变更记录 + 【更新/废弃】标注）”，原型即可按 prd.md 直接开画不再偏。