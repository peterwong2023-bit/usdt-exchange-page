# AI 桌面便签（AI Sticky）- PRD（重写版 v1.0）

## 1. 产品概述
AI Sticky 是一款 macOS 桌面常驻便签应用（dmg 安装），用户可创建多张独立“便签窗口”贴在桌面任意位置。每张便签既可以是静态记事，也可以绑定实时数据源（币价/股票/交通/天气），并通过轻量规则引擎实现“变色/角标”低打扰告警，同时提供告警历史复盘与数据源健康可视化，避免无声失败。

关键产品原则（已定）
- 数据源接入：用户自备 API Key；客户端直连数据源；产品提供 Key 向导与健康检查，不提供代付额度。
- 告警默认：默认不发系统通知、不播放声音；仅便签变色/角标；用户可显式开启通知通道。
- 交通数据源：允许替代源，不强依赖 Google Maps；采用 Provider 可插拔架构。

## 2. 目标与成功标准
### 2.1 业务目标
- 让用户把“高频查看的信息”搬到桌面低打扰常驻，减少在 App/网页间切换成本。
- 在“模板型 widgets”与“静态便签”之间建立差异化：规则驱动 + 可复盘告警 + 可观测数据健康。

### 2.2 产品成功标准（v1.0）
- 新建任意模板便签（A~E）平均用时 ≤ 20 秒（含选择标的/地点的最小配置）。
- 多便签稳定：常驻 20 张便签（其中 5 张高频刷新）无明显卡顿；请求合并生效（同数据源同标的只请求一次）。
- 无声失败为 0：断网/限流/Key 失效必须在便签 Footer 明示。
- 告警闭环可用：触发历史可查、可点击查看快照；去重频控有效，不产生“刷屏式告警”。

## 3. 目标用户与核心场景
### 3.1 用户画像
- 盯盘用户：关心波动与阈值触发；要能复盘“何时触发、触发值多少、当时数据快照”。
- 通勤用户：关心 ETA 是否超阈值；希望在出发前自动更频繁刷新，但平时低打扰。
- 碎片记录用户：随手记；希望一键整理成要点/待办，保留原文可回滚。
- 极客用户：愿意配置 Key、刷新频率与规则；希望可替换数据源，后续期待配置导入。

### 3.2 Top 任务
- 3 秒内定位到某张便签（搜索/切换）。
- 1 分钟内搭建“盯盘 + 通勤 + 天气 + 记事”桌面信息组。
- 在异常发生时（价格波动/ETA 超阈值/降雨概率升高）能被“颜色”感知；需要时打开告警中心查看详情。

## 4. 版本范围（Scope）
### 4.1 v1.0 做
- macOS dmg 安装、常驻运行
- 多窗口便签引擎：拖拽/缩放/置顶/透明度/吸附/锁定；重启与休眠后布局恢复
- 模板便签 5 类：A 币价、B 股票、C 交通、D 天气、E 记事
- 全局刷新调度器：请求合并、可见优先、后台降频、失败退避、暂停全部刷新
- 规则引擎（轻量）：阈值触发、时间段触发；动作：变色/角标；记录触发快照
- 告警中心（本地）：历史、详情、去重频控、通道开关（系统通知/声音，默认关闭）
- Footer 健康度：最后更新时间 + 状态（正常/断网/限流/Key 失效）+ 重试入口
- Key 管理与向导：按 Provider 分类管理，试请求校验与修复入口
- 记事 AI 整理：一键将文本整理为要点/待办/标题（保留原文）

### 4.2 v1.0 不做
- 服务端统一代理拉取数据
- 便签市场、可执行脚本生态
- 移动端、跨端同步
- Webhook、自动交易等高风险自动化

## 5. 信息架构与入口（必须极快）
### 5.1 菜单栏（P0）
- 新建便签（打开模板选择器）
- 搜索/切换便签（按名称/类型/标的）
- 告警中心
- 暂停/恢复全部刷新
- Key 管理

### 5.2 全局热键（P0）
- 新建便签
- 搜索便签
- 循环切换便签（上一个/下一个）

验收：仅键盘可完成“新建 → 搜索 → 聚焦到某张便签”。

## 6. 核心对象模型（产品层）
- 便签 Note
  - type：market/stock/traffic/weather/note
  - dataProvider：某个 Provider 实例 + 配置
  - viewMode：卡片/一行/列表（v1.0 可先卡片为主，其他预留）
  - rules：0..n 条规则
  - alertPolicy：通知通道开关（默认关闭）
  - status：健康状态 + 最后更新时间
- 告警 Alert
  - noteId、ruleId、triggerAt、snapshot、dedupeKey、dedupeWindow、delivery（通知/声音是否开启）、deliveryResult

## 7. 功能需求（v1.0 详细）

## 7.1 多窗口便签引擎（P0）
功能点
- 新建/关闭/复制便签
- 拖拽移动、缩放大小、置顶开关
- 透明度、主题色
- 吸附屏幕边缘；锁定位置（避免误触）
- 状态恢复：重启/崩溃恢复/休眠唤醒后位置大小层级一致

验收
- 重启后 100% 恢复：位置、大小、置顶、锁定、透明度
- 锁定状态下不可被拖动（需明确解锁入口）

## 7.2 模板便签（P0）
### A 币价便签（Market）
- 配置：交易对/标的、数据源 Provider、刷新频率（预设档）
- 展示：现价、涨跌幅、更新时间（可选迷你走势：若成本高可延后）
- 规则建议默认模板：24h 跌幅 > X%（默认不启用，给快捷开关）

验收
- Key 正确时首屏数据 ≤ 10 秒
- Key 缺失/失效时：Footer 显示 Key 失效，并提供“一键去修复”

### B 股票便签（Stock）
- 配置：股票代码、Provider、刷新频率（盘中/盘后可用默认策略）
- 展示：现价、涨跌幅、更新时间

验收同上

### C 交通便签（Traffic）
- 配置：起点/终点（定位或手动地点）；Provider 可选；刷新频率
- 展示：ETA、拥堵等级、更新时间
- 时间段触发：例如工作日 17:30-19:00 自动提高刷新档位（用户可关）

验收
- 未授权定位：必须支持手动地点创建
- Provider 不可用：显示上次成功快照 + 提示切换 Provider

### D 天气便签（Weather）
- 配置：定位或城市；Provider；刷新频率
- 展示：温度、降雨概率、更新时间

### E 记事便签（Note）
- 本地输入：纯文本/Markdown
- AI 整理：输出结构化（标题/要点/待办），保留原文、支持回滚
- 不强制云同步

验收
- 整理结果 ≤ 3 秒返回（超时需降级提示）
- 不丢失原文与关键数字/时间（测试用例覆盖）

## 7.3 Footer 健康度（P0，强制一致）
显示项（固定在便签底部）
- 最后更新时间（相对时间：例如“2 分钟前”）
- 状态：
  - 正常
  - 断网
  - 限流/配额不足
  - Key 缺失/Key 失效
- 操作：
  - 重试
  - 去修复 Key（若相关）
  - 暂停该便签刷新（可选，若 v1.0 来不及可放全局暂停）

验收
- 断网时 5 秒内状态变化可见
- 恢复网络后自动退避重试并恢复到“正常”

## 7.4 全局刷新调度器（P0）
能力
- 请求合并：同 Provider + 同参数（例如同标的）多便签共享一次请求结果
- 优先级：
  - 可见便签优先
  - 不可见/被遮挡降频（最少降到 15/60 分钟档，具体由 Provider rateLimitHint 决定）
- 失败退避：指数退避，避免配额雪崩
- 一键暂停全部刷新（菜单栏）

验收
- 同一标的同时存在 3 张便签：网络请求次数按 1 次计（可观测日志或开发面板验证）
- 失败时不出现高频重试风暴

## 7.5 规则引擎（P0，轻量可用）
触发器（v1.0）
- 阈值触发：数值 > / < 某阈值（涨跌幅、ETA、降雨概率等）
- 时间段触发：在某时间段内切换刷新档位或启用某规则

动作（v1.0）
- 便签变色（背景/边框）
- 角标（例如红点/感叹号）
- 生成告警事件（写入告警中心）

规则执行与快照
- 每次触发必须记录快照：触发值、阈值、数据时间戳、Provider、便签 id

验收
- 同阈值在波动区间内不会疯狂重复触发（需结合去重频控策略）

## 7.6 告警中心（P0，最小闭环）
内容
- 列表：时间、便签、规则名、触发值、当前状态（已去重/已静默等）
- 详情：阈值条件、触发快照（含数据源时间戳）、最近 N 次相关触发（可选）

去重频控（v1.0 默认策略）
- 同一 noteId + ruleId 在 dedupeWindow（默认 10 分钟，可配置）内只记录一次“主告警”，其余归为“合并次数”
- 频控影响“通知发送”，但不影响“便签变色”（变色可持续显示到用户手动清除或条件解除，具体由规则决定）

通知通道
- 系统通知：默认关闭
- 声音：默认关闭
- 支持全局开关 + 便签级开关

验收
- 默认不弹系统通知
- 用户开启后，触发才发送；去重后不会刷屏

## 7.7 Key 管理与向导（P0，因“用户自备 Key”升级）
Key 管理
- 按 Provider 分类（Market/Stock/Traffic/Weather/AI）
- Key 状态：未配置/有效/失效/权限不足/配额不足
- 支持备注与启用/停用

Key 向导（创建便签时）
- 检测：若缺 Key → 引导页说明“去哪获取、需要哪些权限、粘贴后试请求校验”
- 校验成功后自动返回创建流程

验收
- Key 错误时：给出可理解错误（失效/权限不足/配额不足），并提供直接修复入口

## 8. Provider 可插拔架构（产品要求）
目标：交通/天气等不强绑单一供应商，降低合规与可用性风险。

Provider 最小接口（产品层定义）
- fetch(config) → 返回结构化数据 + dataTimestamp
- healthCheck(key/config) → 返回状态码（OK/INVALID_KEY/RATE_LIMIT/OFFLINE/UNKNOWN）
- rateLimitHint → 建议刷新档位与最大频率

v1.0 落地要求
- TrafficProvider 至少支持 2 个可选实现（哪两个由研发评估；PRD 要求是“可切换”，不锁死 Google）
- 切换 Provider 不影响：便签布局、规则、告警历史（仅数据源变更）

## 9. 非功能性要求（硬指标）
- 性能：20 张便签常驻，应用无明显卡顿；重复请求合并有效
- 稳定性：断网不崩溃；展示上次成功快照与最后更新时间
- 恢复：重启/休眠唤醒后布局不丢
- 可观测：Key 失效/限流必须显式展示，禁止“静默停更”
- 安全：Key 本地加密存储；不上传 Key；默认不采集用户数据内容（如需埋点必须开关与匿名化）

## 10. 埋点与指标（建议，若 v1.0 做最小）
- 新建便签完成率（按类型）
- Key 向导完成率、Key 校验失败原因分布
- 便签平均数量、活跃便签数
- 告警触发次数、去重合并次数、用户开启通知比例
- 健康状态分布（断网/限流/Key 失效占比）

## 11. 风险与对策
- 风险：用户自备 Key 门槛导致转化下降  
  对策：Key 向导 + 试请求校验 + 允许“占位便签”（先建布局与规则，Key 补齐自动激活）。
- 风险：高频刷新耗电/限流  
  对策：全局调度、合并请求、不可见降载、退避重试、全局暂停。
- 风险：默认不通知导致用户错过  
  对策：首次触发时在便签内出现一次性提示“可开启通知通道”，由用户显式授权。

## 12. 验收标准（v1.0）
1) 安装与启动
- dmg 安装成功；首次启动不崩溃；菜单栏入口可用

2) 创建与首屏
- A~E 任意模板 2 步内创建；Key 正确且网络正常时 ≤ 10 秒出首屏数据

3) 健康度与失败处理
- 断网：Footer 显示断网 + 最后更新时间
- Key 失效：Footer 显示 Key 失效 + 一键去修复
- 恢复后：自动重试并回到正常状态

4) 告警闭环
- 阈值触发：便签变色/角标；告警中心新增记录；详情可见快照
- 去重频控：同规则 10 分钟内多次触发不刷屏（合并次数可见）
- 默认不通知：未开启通知时不发系统通知/声音

5) 多便签与性能
- 20 张便签常驻（5 张高频）运行稳定；同标的合并请求可验证

6) 状态恢复
- 重启/休眠唤醒后：便签位置大小置顶锁定恢复一致

## 13. 里程碑建议（供排期用）
- M1：便签窗口引擎 + 状态恢复 + 菜单栏/热键骨架
- M2：Provider 框架 + A/B/D 数据拉取 + Footer 健康度
- M3：C 交通 Provider（可切换）+ 全局调度器（合并/降频/退避）
- M4：规则引擎 + 告警中心（历史/去重/通道开关默认关闭）
- M5：E 记事 + AI 整理 + 全量验收与压测

（如需把“P1：便签内问答/RAG”纳入下一个迭代，再单开 v1.1 PRD 附录。）