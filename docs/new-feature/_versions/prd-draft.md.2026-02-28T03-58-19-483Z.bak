## 1) 设置按钮不能点：必须补的点击链路（P0）
便签右上角齿轮（设置）至少要支持两条可点击路径：
- 点击齿轮 → 打开“便签设置 Popover”（含：刷新/规则/通知通道/数据源/Key）
- 在 Popover 内点击“数据源/Key”→ 跳 Key 向导/Key 管理（至少 Overlay 弹层）

同时把“更多（…）菜单”做成可点：
- 置顶/锁定/关闭/复制/透明度（至少能点出菜单与状态勾选变化）

> 评审要看的是：用户遇到 Key 失效/限流时，能否通过设置一路修复回来；不需要真逻辑，但路径要走通。

## 2) 便签不能拉大小：v1.0 应该支持“可缩放窗口”（P0）
原型里需要明确并表现出“可缩放”交互，否则开发会按默认窗口处理，体验容易跑偏。

建议定死如下行为（macOS 原生习惯，成本最低）：
- **缩放方式**：右下角 Resize handle（或窗口边缘可拖拽），鼠标 hover 显示双向箭头光标
- **最小尺寸**：例如 220×140（避免内容挤爆）
- **最大尺寸**：不强制（或限制在屏幕可视范围内）
- **内容自适应**：Body 区域随窗口伸缩；Header/Footer 高度固定
- **可选开关**（更多菜单）：`锁定大小`（锁定后禁用缩放）

原型需要补一张“缩放态”画面：显示右下角拖拽点/边缘 hover 状态，并在说明标注最小尺寸。

## 3) 可点击原型的最低交付清单（给 @驴毕加索）
在现有 Frame 基础上，补 4 组关键连线与状态页即可让你“点得动”：
1. **Frame：便签窗口 → 设置 Popover**（齿轮可点，Popover 内 Tab 可切）
2. **Frame：更多菜单**（… 可点，置顶/锁定勾选有状态）
3. **Frame：缩放态**（展示 resize handle + 最小尺寸说明）
4. **Frame：失败态 → 修复链路**  
   - Footer 状态（Key 失效/缺 Key/限流/断网）每种都能点“修复/重试/切 Provider”跳到对应弹层

[便签] 原型评审必须“点得动三处”：便签齿轮设置、更多菜单、Footer 失败态的修复入口；同时补一张缩放态并定义最小尺寸与自适应规则。