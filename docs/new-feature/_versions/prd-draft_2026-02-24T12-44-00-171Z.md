发生覆盖的原因：我上一条输出只有 `[便签] ...` 两行，系统把这次输出当成“要写入文档的内容”，于是把目标文档正文替换成了便签文本。便签标记应该只用于看板提取，不应出现在“写文档”的输出里。

处理方式需要你确认 1 个信息，我才能把正文完整恢复回去：

- 被覆盖的是哪个文件？给我文件名或路径（常见是这两个之一）  
  1) `docs/new-feature/agent-team-collaboration/prd-draft.md`  
  2) `docs/open-in-preview/prd.md` 或 `docs/open-in-preview/spec.md`

你回一句“覆盖的是 X”，我会立刻输出该文件的**完整正确正文**（以 `#` 开头，带 WRITE-MARK），系统会自动把文档恢复。

后续避免再发生的规则我会严格执行：
- **贴看板**：单独一条回复，仅 1-2 条 `[便签]`，不夹带文档正文  
- **写文档**：正文里绝不出现 `[便签]` 标记（改用普通“要点/结论”段落）