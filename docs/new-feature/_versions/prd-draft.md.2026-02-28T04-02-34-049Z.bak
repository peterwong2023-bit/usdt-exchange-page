## 一、数据监控类（最贴合你现在 A/B/C/D 的延伸）
1) 汇率/黄金/油价模板  
- 数据：FX/Gold/Oil 公共行情源（可选 Key）  
- 默认规则：日内涨跌超阈值变色；每日固定时间生成一句话摘要（可选）

2) 新闻热榜/自选股新闻模板（“解释层”素材池）  
- 数据：RSS/聚合源/用户自定义 RSS（几乎不用 Key）  
- 默认规则：出现关键词（裁员/监管/黑客/财报）角标；点击展开列表

3) 日历/会议倒计时模板  
- 数据：本地日历（系统权限）  
- 默认规则：会前 10 分钟变色；冲突会议角标（不需要联网 Key）

4) 航班/火车/出行行程模板（行程卡）  
- 数据：用户粘贴行程或邮件解析（先本地解析，联网查询可后置）  
- 默认规则：延误/登机口变更角标；T-30min 变色

5) 包裹物流模板  
- 数据：物流聚合（通常要 Key，可先做“粘贴单号+公开接口/邮件解析”）  
- 默认规则：状态变更角标；派送中变黄、签收变绿

## 二、办公协作/研发类（极客用户高频、也很好做“低打扰常驻”）
6) GitHub PR/Issues 模板  
- 数据：GitHub Token（用户自备 Key 逻辑一致）  
- 默认规则：有 Review 请求/CI 失败变红；超时未处理角标

7) CI/服务健康模板（Status Page / Uptime / Ping）  
- 数据：URL 探活 + JSON 状态接口（可插拔 Provider）  
- 默认规则：不可达/延迟超阈值变红；告警中心记录故障窗口

8) Jira/Linear/飞书待办模板（看板切片）  
- 数据：各家 API Token  
- 默认规则：今天到期变红；阻塞状态角标

## 三、桌面生产力类（不依赖外部数据源，能降低“自备 Key”门槛）
9) 番茄钟/专注模板  
- 数据：本地计时器（无 Key）  
- 默认规则：专注中便签半透明；结束角标（默认不系统通知，符合你决策）

10) 剪贴板历史/常用片段模板  
- 数据：本地剪贴板（权限）  
- 默认规则：自动归类（邮箱/地址/代码段）；一键复制

11) 倒计时/纪念日模板  
- 数据：本地时间  
- 默认规则：T-1 天变黄、T-1 小时变红

12) “仪表盘总览”模板（把多个便签的关键值压成一张）  
- 数据：读取本应用内其它便签的最新快照（不新增 Key）  
- 默认规则：任一子项触发则总览角标；点开跳转对应便签（很适合展示差异化）

## 四、非常规但差异化强（建议 P1/P2）
13) “反脆弱”模板：数据源健康墙  
- 展示所有 Provider 的 Key 状态/限流/错误率/最后成功时间  
- 价值：把竞品痛点（无声失败）产品化成“可运维”

14) “脱敏模式”模板（公共场景保护资产隐私）  
- 一键把金额/价格模糊，仅保留红绿与趋势；临时解锁 10 秒

[便签] 推荐新增模板优先级：①日历倒计时（无 Key、强常驻）②GitHub PR/CI（Token 直连、告警闭环明显）③RSS 新闻（几乎零门槛、给“原因解释”供料）④总览便签（复用快照、差异化强）。

需要你拍板：更偏“生活效率”（日历/物流/航班）还是“极客工作台”（GitHub/CI/Jira）先上，我再把前 3 个模板的字段、默认规则、原型细节补成可开发规格。