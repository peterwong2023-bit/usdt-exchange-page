# AI Sticky 低保真原型（v1.0 骨架稿）

## 0. 原型范围（本次给你看的）
- 入口：菜单栏下拉、全局搜索切换器、新建便签流程
- 核心：便签窗口（A~E 模板通用骨架 + Footer 健康度）
- 配置：便签设置面板（规则/刷新/通知通道/Provider/Key 修复）
- 闭环：告警中心（历史/详情/去重信息）
- Key：Key 管理与向导（试请求校验）

---

## 1. 信息架构（页面/面板清单）
1) 菜单栏下拉（P0）
2) 新建便签：模板选择器（P0）
3) 全局搜索/切换器（P0）
4) 便签窗口（P0，A~E 共用壳）
5) 便签设置 Popover（P0）
6) 告警中心（P0）
7) Key 管理（P0）
8) Key 向导（P0）

---

## 2. 菜单栏下拉（入口原型）
布局（下拉面板 320px 宽）：
- 顶部：AI Sticky + 当前状态（刷新运行中/已暂停）
- 主操作：
  - + 新建便签…
  - 搜索/切换便签…（带快捷键提示）
  - 告警中心（角标显示未读数）
- 快捷操作：
  - 暂停全部刷新 / 恢复全部刷新（toggle）
  - Key 管理
- 底部：设置… / 退出

交互要点
- “暂停全部刷新”后：所有便签 Footer 统一显示“已暂停（点击恢复）”
- 告警中心角标：仅表示“未查看的新告警条目数”（不等于未读通知）

---

## 3. 新建便签：模板选择器（2 步创建的关键）
形式：弹窗/浮层（居中），上方搜索，下面模板卡片

模板卡片（第一行 5 张）
- A 币价（Market）
- B 股票（Stock）
- C 交通（Traffic）
- D 天气（Weather）
- E 记事（Note）
-（可选）自定义（灰态/“即将支持”或 v1.0 仅做“从模板复制后改配置”）

点击模板后进入“最小配置页”（同一个弹窗第二步）：
- A：交易对输入框（支持搜索下拉）+ Provider 下拉（默认上次）+ 刷新档位（1/5/15/60min）
- B：股票代码/名称输入框 + Provider + 刷新档位（盘中/盘后预设）
- C：起点/终点（定位按钮 + 手动输入）+ Provider + 刷新档位
- D：城市/定位 + Provider + 刷新档位
- E：标题（可选）+ 直接创建（创建后在便签里输入）

底部按钮：取消 / 创建（创建后立即出现便签窗口）

失败分支（强制可见）
- 若缺 Key：创建按钮仍可点，但创建后便签显示“缺 Key”状态，同时弹出 Key 向导（见第 8 节）

---

## 4. 全局搜索/切换器（像 Raycast 那样快）
触发：全局热键
布局：顶部输入框 + 下方列表（每行一张便签）
- 每行：图标（类型）+ 名称 + 关键标的（如 BTC/TSLA）+ 当前状态点（绿/黄/红/灰）+ 最近更新时间
- 支持：
  - 回车：聚焦该便签并置顶闪一下边框
  - Cmd+Enter：打开该便签设置
  - 方向键上下切换
  - 关键词匹配：名称/类型/标的

---

## 5. 便签窗口（A~E 通用外壳）
低保真结构（所有便签一致，最大化“可预期”）：

[Header]
- 左侧：类型图标 + 标题（可编辑）
- 右侧：三按钮
  1) 刷新（手动）
  2) 设置（齿轮，打开设置 Popover）
  3) 更多（…：复制、锁定、置顶、透明度、关闭）

[Body]
- A/B：主数值（价格）+ 次数值（涨跌幅）+（可选）迷你趋势
- C：ETA 大字 + 拥堵等级条 +（可选）建议出发时间
- D：温度大字 + 降雨概率 +（可选）未来 3 小时小条
- E：编辑区（Markdown/纯文本）+ “AI 整理”按钮（右上角小按钮或底部工具条）

[Footer（强制一致）]
- 左：最后更新：2 分钟前
- 中：状态 Chip（正常/断网/限流/Key 失效/已暂停）
- 右：动作
  - 重试（仅失败态出现）
  - 修复 Key（Key 失效/缺失出现）
  - 告警角标（若当前处于触发状态，显示红点；点击跳告警中心并定位）

便签状态视觉
- 规则触发：边框/背景变色（红/绿/黄）+ 右上角角标
- 默认不系统通知：触发仅改变便签本体（符合你已定策略）

---

## 6. 便签设置 Popover（从便签右上角齿轮进入）
分 5 个 Tab（或分区折叠，保证 v1.0 可控）：

### 6.1 概览
- 名称、类型、标的/地点摘要
- 当前 Provider
- 最近一次成功数据时间戳
- “打开告警历史”（跳告警中心并筛选该便签）

### 6.2 刷新
- 刷新频率档位（1/5/15/60min）
- 可见优先开关（默认开）
- 后台降频开关（默认开）
- 失败退避说明（只读）

### 6.3 规则（v1.0 轻量）
规则列表（每条：开关 + 条件摘要 + 颜色动作）
- 新增规则：选择触发器
  - 阈值：字段（涨跌幅/价格/ETA/降雨概率）+ 运算符 + 阈值
  - 时间段：工作日/周末 + 起止时间 + 动作（提高刷新档位/启用某阈值规则）
- 动作：变色（红/绿/黄）+ 角标开关
- 去重窗口（默认 10 分钟，可改）

### 6.4 通知通道（默认全关）
- 系统通知：toggle（默认关）
- 声音：toggle（默认关）
- 说明文案：仅当开启时才会在告警触发且未被去重时发送

### 6.5 数据源（Provider + Key）
- Provider 下拉（Traffic/Weather 必须支持可切换）
- Key 状态：有效/失效/缺失/配额不足
- 按钮：修复 Key（跳 Key 向导）/ 测试连接（试请求）

---

## 7. 告警中心（最小闭环）
入口：菜单栏 / 便签 Footer 角标

布局：左列表 + 右详情（典型 master-detail）
- 左：告警列表
  - 每条：时间 + 便签名 + 规则名 + 触发值 + 合并次数（如 x5）
  - 顶部筛选：全部 / 仅该便签 / 仅未查看
- 右：告警详情
  - 触发原因摘要（条件、阈值）
  - 快照：触发值、数据时间戳、Provider
  - 去重信息：窗口期、合并次数
  - 操作：标记已查看 / 清空该便签历史 / 跳转到便签

关键点
- 告警中心记录不依赖系统通知；即使默认不通知也必须完整留痕

---

## 8. Key 管理 + Key 向导（因为你选“用户自备 Key”必须做强）
### 8.1 Key 管理（设置页）
Provider 分组列表：
- Market / Stock / Traffic / Weather / AI（若 v1.0 只用本地模型或不集成可先隐藏 AI）
每项显示：
- Key 名称（用户备注）
- 状态（有效/失效/配额不足）
- 最近一次检测时间
操作：
- 新增 Key
- 编辑/删除
- 测试连接（试请求）

### 8.2 Key 向导（创建便签缺 Key 或 Key 失效时弹出）
三步式：
1) 选择 Provider（自动预选本次便签所需）
2) 指引链接（去哪获取 Key）+ 输入框粘贴 Key
3) 试请求校验结果（成功/失败原因：401/429/网络错误）+ 保存并返回

保存后：回到原便签，自动重试刷新，Footer 状态转“正常”

---

## 9. 给 @驴毕加索 的原型交付格式（建议）
- Frame 01：菜单栏下拉
- Frame 02：模板选择器（模板列表）
- Frame 03：最小配置页（以 A 币价为例，其他复用）
- Frame 04：全局搜索切换器
- Frame 05：便签窗口（A/B/C/D/E 各 1 张，复用同壳）
- Frame 06：便签设置 Popover（5 个 Tab）
- Frame 07：告警中心（列表+详情）
- Frame 08：Key 管理
- Frame 09：Key 向导（3 步）

如果需要下一步把它画成可点击原型：指定一个“视觉基线”（macOS 原生风格 vs 更极客的半透明卡片风），再定交互细节（锁定/点击穿透是否纳入 v1.0）。