## 模板库扩展清单（新增 7 个）

### 1) 汇率/黄金/油价（FX/Gold/Oil）模板
- 数据源（Provider）：MarketProvider 扩展品类（FX / Metals / Energy）
- Key：可选（优先公共免费源；若需高频/更稳，再引导用户填 Key）
- 展示字段（Body）
  - 标的名称/代码（如 USD/CNY、XAU/USD、WTI）
  - 当前价、日内涨跌（数值+百分比）、更新时间
  - 可选：迷你趋势（后续）
- 默认刷新
  - 前台 1/5 min 档；后台 15/60 min 档（由 rateLimitHint 控制）
- 默认规则（关闭状态，给“一键启用”）
  - 日内涨跌幅 > X%：变红/变绿
  - 每日固定时间（如 18:00）生成“一句话摘要”（不弹通知，仅写入便签下方一行）
- 失败态
  - 限流/断网/Key 失效：显示上次成功快照 + Footer 状态 + 重试

### 2) 新闻热榜 / 自选关键词 RSS 模板
- 数据源（Provider）：NewsProvider（RSS/Atom + 简单聚合）
- Key：默认不需要（用户粘贴 RSS 链接即可）
- 配置字段
  - 来源：预置源（可选）+ 自定义 RSS URL 列表
  - 关键词：用户自定义（如 裁员/监管/黑客/财报），支持包含/排除
- 展示字段
  - 列表：标题、来源、发布时间（相对时间）
  - 顶部：今日热度摘要（可选：仅本地规则统计，不用 AI）
- 默认刷新：5/15 min（新闻不建议 1 min）
- 默认规则
  - 命中关键词：角标（不通知）
  - 24h 内新增数量 > N：变黄（提示“信息密度高”）
- 关键交互
  - 点击条目：展开摘要（无 AI）+ “打开原文”
- 失败态
  - RSS 拉取失败：显示“上次成功拉取时间 + 错误原因（URL/超时）”

### 3) 日历 / 会议倒计时模板
- 数据源：本地日历（EventKit）
- 权限：日历读取权限（无外部 Key）
- 配置字段
  - 日历选择（默认全部）
  - 关注类型：最近一场会议 / 今天所有会议（两种模式）
- 展示字段
  - 最近会议标题、开始时间、倒计时（T-xx）
  - 冲突标识：与下一场重叠则显示“冲突”角标
- 刷新策略
  - 常态 1 min（本地计算，不走网络）
  - 临近会议（T-30min）可提高 UI 刷新频率（仍本地）
- 默认规则
  - 会前 10 分钟：变黄；会前 1 分钟：变红
  - 冲突会议：角标
- 失败态
  - 未授权：便签 Body 显示“需要日历权限（点击授权）”，Footer 状态为“权限缺失”

### 4) 航班/火车/出行行程模板（行程卡）
- 数据源：本地粘贴解析（优先）+ 可选联网查询（P1）
- 配置/输入
  - 用户粘贴：航班号/车次号/邮件文本（先做“粘贴即解析”）
  - 手动补充：出发地/到达地/时间（兜底）
- 展示字段
  - 行程摘要：班次、出发/到达、计划时间
  - 状态：已确认/待出发/已结束（本地推导）
- 默认规则
  - T-30min：变黄；T-10min：变红（不通知）
  - 若未来接入在线状态：延误/登机口变更 → 角标
- 刷新策略
  - 纯本地：每分钟本地倒计时刷新
  - 在线状态（后续）：临近出发提高刷新，远期降频
- 失败态
  - 解析失败：提示“未识别到行程信息（点击查看示例格式）”

### 5) 包裹物流模板
- 数据源：先做“粘贴单号 + 公开/低门槛查询源”或“邮件解析”（按技术可行性二选一落地）
- Key：尽量不强制；若某 Provider 必须 Key，则走 Key 向导
- 配置字段
  - 快递公司（可自动识别/手选）
  - 运单号
- 展示字段
  - 当前状态（揽收/运输中/派送中/签收）
  - 最近一条轨迹（时间+地点+事件）
- 默认规则
  - 状态变化：角标
  - 派送中：变黄；签收：变绿
- 刷新策略
  - 15/60 min（物流无需高频）
- 失败态
  - 单号无效/查询失败：明确错误类型（单号不存在/频繁查询/Provider 不可用）

---

### 6) 番茄钟/专注模板（桌面生产力）
- 数据源：本地计时器（无 Key）
- 配置字段
  - 专注时长（默认 25min）、休息时长（5min）、循环次数（可选）
- 展示字段
  - 剩余时间、大状态（专注/休息/暂停）
- 默认规则（符合“默认不通知”）
  - 专注中：便签半透明（可选）
  - 结束：角标 + 变色（不弹系统通知，用户可在设置里打开）
- 关键交互
  - 开始/暂停/重置按钮（必须可点）
- 失败态：无（仅需保证重启后状态策略：默认恢复为“暂停并保留剩余时间”或“重置”，建议二选一并写死）

### 7) 纪念日/倒计时模板
- 数据源：本地时间（无 Key）
- 配置字段
  - 事件名称、目标日期（支持农历先不做）
- 展示字段
  - D-天数 / 剩余小时（可选）
- 默认规则
  - T-1 天：变黄；T-1 小时：变红
- 刷新策略：每小时刷新（临近 24h 内每分钟本地刷新）

---

## 统一要求（这些需要在原型/开发里“画死”）
- 每个模板都必须接入：Footer 健康度（最后更新时间/状态/重试/修复 Key）
- 默认告警策略不变：仅变色/角标；通知通道默认关闭
- 每个模板都必须能走通“无 Key/缺权限/解析失败”的可点击修复链路

## 建议排期优先级（按 ROI/门槛）
1) 日历倒计时（无 Key、强常驻）  
2) 番茄钟 + 纪念日（纯本地、补齐“无 Key 也好用”）  
3) RSS 新闻（零门槛、给解释层素材）  
4) 汇率/黄金/油价（复用行情 Provider）  
5) 物流、行程（解析/Provider 不确定性更高，放后）

[便签] 模板扩展已确定 7 个：FX/Gold/Oil、RSS 新闻、日历倒计时、行程卡、物流、番茄钟、纪念日；统一要求必须覆盖失败态与修复链路（权限/Key/解析失败）。