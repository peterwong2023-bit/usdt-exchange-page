## 1) 现原型的完成度判断
- 已具备：入口（菜单栏/热键/新建）、便签通用壳、设置分区、告警中心框架、Key 管理/向导框架。
- 不足：大量“必须发生的分支状态”没画出来（缺 Key、限流、断网、去重、Provider 切换后数据迁移等），这会直接影响可用性与评审通过率。

## 2) 必须补齐的原型缺口（按优先级）
### P0-1 失败与降级态（没画全就等于没做）
1. 便签 Footer 状态全套：正常/断网/限流(429)/Key 缺失/Key 失效(401)/配额不足  
   - 每种状态对应的按钮不同（重试/修复 Key/切 Provider/查看详情）。
2. “上次成功快照”展示：失败时 Body 是否置灰？是否显示“上次成功数据 + 时间戳”？需要原型明确。

### P0-2 告警闭环细化（TradingView 那套最小闭环要落到交互）
3. 去重窗口的表现：  
   - 同一规则 10 分钟内多次触发：告警列表显示“xN 合并次数”；详情页展示合并明细或至少展示合并计数+首末时间。
4. “未查看”定义与行为：点击告警详情后是否自动标记查看？需要原型定死。
5. 告警与“当前触发态”的关系：便签右下角红点何时出现/消失（条件解除 vs 用户手动清除）。

### P0-3 Key 向导与试请求闭环（你选“用户自备 Key”，这里是生死线）
6. Key 向导失败态：401/403/429/网络错误分别给什么文案、下一步按钮是什么（重试/换 Key/去看配额）。
7. Key 与 Provider 的绑定策略：一个 Provider 多个 Key 怎么选（默认/手动选择/按便签选择）。原型需体现“该便签使用哪个 Key”。

### P0-4 Provider 可插拔（交通不强绑 Google 的核心点）
8. Traffic/Weather 的 Provider 下拉切换后：  
   - 是否自动试请求？失败如何回滚？  
   - 切换是否影响历史告警归属？（建议不影响，告警仍归便签）
9. “切换 Provider 建议”：当检测到某 Provider 持续失败时，是否在便签里提示“一键切换备选源”。

### P0-5 全局刷新调度的用户可见控制（否则只存在于 PRD）
10. 菜单栏“暂停全部刷新”后的视觉回执：每张便签 Footer 显示“已暂停”，点击恢复仅恢复该张还是全局？原型要统一。
11. 单便签暂停（可选但建议有）：在便签更多菜单里“暂停此便签刷新”。

## 3) 建议把原型补到“可开发级”的交付件
给 @驴毕加索 的追加 Frame 清单（在你现有 01-09 基础上加）：
- Frame 10：便签 Footer 五种失败态（每种一张）
- Frame 11：告警去重合并态（列表 xN + 详情展示）
- Frame 12：Key 向导失败态（401/429/断网）+ 修复后自动回到便签成功态
- Frame 13：Provider 切换流程（切换→试请求→成功/失败回滚）
- Frame 14：暂停全部刷新/恢复的全局与单便签反馈

## 4) 你现在需要做的评审选择（影响原型收敛速度）
只需要拍板 2 个细节，原型就能快速封版：
1) 失败时 Body 展示策略：  
   - A 显示上次成功快照（推荐） vs B 置空+错误占位
2) 触发态清除策略：  
   - A 条件解除自动恢复颜色 vs B 必须用户手动“清除触发”（更像告警确认）

[便签：风险提醒] 原型目前欠缺“失败降级/告警去重/Key 向导失败态/Provider 切换回滚”四块关键分支，不补齐会导致开发阶段大量口头约定、上线体验不可控。