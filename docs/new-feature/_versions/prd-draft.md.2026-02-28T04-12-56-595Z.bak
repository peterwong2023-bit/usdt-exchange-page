# 模板细化功能清单（功能全量脑暴版，用于逐个核对）

## 0) 全模板通用能力（所有便签一致）
- 窗口能力：拖拽、缩放（最小尺寸）、置顶、锁定位置、透明度、复制便签、关闭/恢复
- 标题：可编辑；支持重命名
- 刷新：手动刷新按钮；自动刷新档位（1/5/15/60）；可见优先、后台降频、全局暂停
- Footer 健康度：最后更新时间（相对时间）+ 状态（正常/断网/限流/Key缺失/Key失效/权限缺失/已暂停）+ 对应动作（重试/修复Key/授权/切Provider/查看告警）
- 设置面板（Popover）：概览 / 刷新 / 规则 / 通知通道 / 数据源&Key
- 规则引擎：阈值触发、时间段触发；动作：变色、角标、写入告警中心
- 告警中心：列表+详情；去重窗口（默认10分钟）；合并次数；跳回便签
- Provider/Key：可切换数据源（至少交通/天气）；Key 向导（试请求校验）；Key 状态可见
- 快照与离线：联网模板失败时展示“上次成功快照 + 时间戳”（不允许空白）
- 数据导出/清理（后续）：清空缓存、清空告警历史（便签级/全局级）

---

## A) 币价（Market）模板——可想到的功能
- 展示
  - 当前价（主）、涨跌额/涨跌幅（次）、24h高低、24h成交量（可选）
  - 迷你趋势（可选：1h/24h）
  - 更新时间 + 数据源名称
- 配置
  - 交易对/标的搜索（BTC/ETH/SOL…）
  - 计价货币（USD/CNY/USDT 等）
  - 单位/后缀自定义（满足“通用行情卡单位不一样”）
  - Provider 选择（交易所/聚合）+ Key 绑定
- 规则/告警
  - 价格突破：>X 或 <X
  - 波动率：n分钟涨跌幅 >X%
  - 触发后变色/角标；写入告警中心；去重合并
- 交互
  - 点击数值：切换显示（价格/涨跌/百分比）
  - 手动刷新、复制便签（同标的不同规则）
- 失败态
  - 断网/限流/Key失效：显示上次成功快照；提供切Provider/修复Key

---

## B) 股票（Stock）模板——可想到的功能
- 展示
  - 现价、涨跌幅、昨收/今开（可选）、成交量（可选）
  - 盘中/盘后状态（可选）
  - 更新时间 + 数据源
- 配置
  - 股票代码/名称搜索（含市场：US/HK/CN）
  - 货币单位/后缀
  - 刷新策略：盘中高频、盘后低频（可一键预设）
  - Provider + Key
- 规则/告警
  - 价格突破、涨跌幅阈值
  - 交易时段内才触发（可选）
- 交互
  - 一键切换“看涨跌幅/看价格”
- 失败态
  - 同 Market（快照+修复）

---

## C) 交通（Traffic）模板——可想到的功能（Provider 可插拔）
- 展示
  - 路线（起点→终点）、ETA（主）、拥堵等级条（次）
  - 建议出发时间（可选：基于当前ETA趋势，后续）
  - 更新时间 + Provider
- 配置
  - 起点/终点：定位选择/手动地点；常用地点（家/公司）快捷填充（可选）
  - 出行方式：驾车/公交/步行（按数据源能力）
  - Provider 切换（Google/替代源）+ Key 绑定
- 刷新策略
  - 工作日时段预设：如 17:30-19:00 提高刷新
  - 后台降频，避免耗电
- 规则/告警
  - ETA > X 分钟（或比平时多 Y 分钟）触发
  - 触发后变色/角标；写入告警中心
- 交互
  - 一键打开地图（外跳到浏览器/地图App，后续）
  - 切Provider时自动试请求；失败回滚到原Provider（原型需体现）
- 失败态
  - 定位未授权：允许手动地点创建；提示授权入口
  - Provider限流/Key失效：切Provider入口优先级高

---

## D) 天气（Weather）模板——可想到的功能（Provider 可插拔）
- 展示
  - 当前温度（主）、体感/最高最低（可选）、降雨概率（次）、风/空气质量（可选）
  - 更新时间 + Provider
- 配置
  - 定位/城市搜索；单位（℃/℉）
  - Provider 切换 + Key 绑定
- 规则/告警
  - 降雨概率 > X%
  - 温度 > / < X（高温/低温）
  - 极端天气关键词（若源支持）→ 角标
- 交互
  - 切换“现在/未来3小时/今日”（v1.0 可只做现在，后续扩展）
- 失败态
  - 断网/Key失效：快照+修复

---

## E) 记事（Note）模板——可想到的功能（纯本地优先）
- 编辑能力
  - 纯文本/Markdown；撤销/重做（基础）
  - 固定宽度/自动换行（可选）
- AI 整理（按钮）
  - 整理为：标题+要点+待办（checkbox）
  - 保留原文、可一键回滚
  - “仅本便签内容”处理（避免跨域）
- 规则/告警（可选但有用）
  - 到期提醒：若识别到日期时间（P1）
  - 关键词高亮（P1）
- 交互
  - 快速输入（全局热键弹出输入条写入指定便签，P1）
- 状态
  - 本地模板无断网/Key；Footer 显示“本地/无刷新”或“本地计算”

---

## P0-1) 日历/会议倒计时（Calendar）模板——可想到的功能
- 展示
  - 下一场会议：标题、开始时间、T-倒计时
  - 可选：地点/会议链接（Join）
- 配置
  - 选择日历源（全部/指定日历）
  - 过滤：仅会议/包含关键词（可选）
- 权限向导（必须）
  - 便签首次创建先出“权限预热页”→ 再触发系统弹窗
  - 拒绝后提供“去系统设置开启”的明确指引
- 规则/告警
  - 会前10分钟变黄，会前1分钟变红（默认不通知）
  - 冲突会议角标（可选）
- 刷新
  - 本地计算每分钟刷新即可（不走网络）
- 失败态
  - PermissionMissing：按钮“点击授权”；授权成功回 Normal 并显示倒计时

---

## P0-2) 番茄钟（Pomodoro）模板——可想到的功能
- 核心
  - 专注/休息计时；开始/暂停/重置
  - 可配置时长：专注/休息（默认25/5）
- 展示
  - 剩余时间大字 + 当前阶段状态
- 通知（默认关闭）
  - 结束时仅变色/角标；用户可开启系统通知/声音（静音默认）
- 状态持久化
  - 重启后：继续计时 or 自动暂停并保留剩余（需要拍板，建议“自动暂停保留剩余”更稳）
- 规则（可选）
  - 连续完成N个番茄显示徽章（P1）

---

## P0-3) 纪念日/倒计时（Anniversary）模板——可想到的功能
- 核心
  - 事件名 + 目标日期
  - 显示：D-天 / 剩余小时（可选）
- 规则/告警
  - T-1天变黄，T-1小时变红（默认不通知）
- 配置
  - 日期选择器；重复（每年/一次性，P1）

---

## P1 预留（先列出来，后面逐个核对时可直接砍/收）
- RSS 新闻热榜：源管理（预置+自定义URL）、关键词过滤、仅标题+短摘要、点击外跳、刷新≥15min、合并请求、失败快照
- “大宗商品/外汇”通用行情：复用 Market 模板，新增 assetType 与单位预设

---

下一步执行方式：你指定从哪个模板开始（建议从“日历/番茄钟/交通”三者里选一个），我把该模板拆成「字段定义 + 设置项 + 默认规则 + 失败态文案 + 原型页面清单 + 验收用例」。