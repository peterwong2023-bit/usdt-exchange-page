### 驴得稳的二次审查意见：

#### 1. 关于“汇率/黄金/油价” (FX/Gold/Oil)
- **老驴拍板**：合并！底层逻辑完全一致，都是 `Symbol -> Price -> Change`。
- **务实建议**：别自己去接几十个 API。找一个支持多品类的公共 Provider（如 Yahoo Finance 或类似聚合源），用户填一个 Key 就能看全品类。
- **简化级联**：UI 统一用“行情卡片”模板，只换图标和单位。

#### 2. 关于“RSS 新闻/热榜”
- **老驴拍板**：**砍掉“关键词 AI 摘要”**。
- **理由**：RSS 源本身就很乱，再加个 AI 实时跑关键词匹配，CPU 绝对原地起飞。
- **务实做法**：只做“标题列表 + 来源”。点击标题直接调系统默认浏览器。我们是便签，不是 RSS 阅读器，别越界。

#### 3. 关于“日历/会议倒计时” (EventKit)
- **老驴拍板**：**这是 P0 中的 P0**。
- **务实建议**：必须支持“一键加入会议”。如果日程里有 Zoom/Teams/腾讯会议链接，便签上直接出一个“Join”按钮。这才是真正的生产力，比看倒计时有用。

#### 4. 关于“行程卡/物流”
- **老驴拍板**：**维持原判，砍掉或放进 P2**。
- **理由**：
    - **物流**：国内顺丰、菜鸟的接口是不开放的，靠爬虫或邮件解析极不稳定。
    - **行程**：隐私权限太重。用户第一次用你的 App，你就要扫人家邮件和日历，信任成本太高。
- **替代方案**：做一个“手动倒计时”模板，用户自己填“14:00 飞上海”，这比自动解析稳得多。

#### 5. 关于“番茄钟/专注”
- **老驴拍板**：**做，但要极简**。
- **务实建议**：不要做复杂的统计图表。就三个状态：专注中、休息中、已完成。
- **关键细节**：支持“置顶且穿透”。专注时便签半透明挂在角落，鼠标点过去不干扰下层操作，这才是极客要的专注。

---

### 最终修订版 PRD 模板清单（老驴签字版）：

| 模板 | 状态 | 核心逻辑 | 备注 |
| :--- | :--- | :--- | :--- |
| **通用行情卡** | **P0** | 汇率/黄金/油价/币/股 | 统一 UI 框架，支持多数据源切换。 |
| **日历倒计时** | **P0** | 本地 EventKit 权限 | 增加“一键入会”快捷按钮。 |
| **番茄钟** | **P0** | 本地计时器 | 支持“点击穿透”模式。 |
| **纪念日** | **P1** | 本地时间计算 | 纯离线，补齐工具属性。 |
| **RSS 情报站** | **P1** | 异步拉取 XML | 仅限标题展示，严禁内置阅读器。 |
| **行程/物流** | **P2** | 外部 API / 复杂解析 | 暂不进入 MVP，仅保留技术调研。 |

[便签] 所有的“本地数据”模板（日历、番茄钟、纪念日）必须做到**零配置启动**。用户拖出来就能用，不需要填任何 Key，这是留住非技术用户的关键。

@驴本事 别在物流接口上浪费时间了，先把 EventKit 的权限申请流程跑通。@驴毕加索 按照这个清单更新原型。