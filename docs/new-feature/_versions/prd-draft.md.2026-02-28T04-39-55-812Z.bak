# AI 桌面便签（AI Sticky）- PRD 定稿（v1.2）

## 0) 已拍板的产品决策（不可再漂移）
1. 数据源策略：用户自备 API Key，客户端直连（不提供代付/服务端代理）。
2. 告警默认策略：默认不发系统通知、不播声音；仅便签变色/角标 + 写入告警中心。用户可显式开启通知通道。
3. 交通/天气等数据源：Provider 可插拔，允许替代源，不强绑 Google。
4. 窗口尺寸策略：  
   - 记事/会议记录：允许自由缩放（有最小尺寸限制）。  
   - 其余模板：严格尺寸（只能 S/M/L/XL 四档切换，禁止拖拽缩放）。
5. 币价趋势线：必须做（Sparkline），固定“近 60 分钟窗口”；采样间隔随刷新频率变化；v1.x 不做多周期切换按钮、不做技术指标（MA/RSI/MACD）。

---

## 1) 需求背景
桌面便签市场普遍存在三类缺口：  
- 静态便签：记录快但信息不更新；  
- Widgets：能更新但缺“告警闭环/失败可观测/可复盘”；  
- 极客仪表盘：强但重、成本高、常驻体验差。  

AI 常驻问答已足够泛化，本产品的差异化必须落在：**实时数据 + 规则告警闭环 + 数据源健康可见 + 低资源常驻**。

---

## 2) 目标用户
- 盯盘用户：桌面一眼看 + 阈值触发 + 可复盘。
- 通勤用户：ETA 超阈值变色提示 + 低打扰。
- 办公用户：会议倒计时 + 快速记事/整理。
- 极客用户：可插拔 Provider + 自备 Key + 多便签组合。

---

## 3) v1.0 范围（Scope）
### 3.1 v1.0 必做（P0）
- macOS 客户端（dmg）、多便签独立窗口引擎：拖拽、置顶、透明度、锁定位置、吸附、重启/休眠恢复布局。
- 模板（P0）：
  1) 币价（Market，含趋势线 L/XL）
  2) 股票（Stock）
  3) 交通（Traffic）
  4) 天气（Weather）
  5) 记事（Note，含 AI 一键整理）
  6) 日历/会议倒计时（Calendar，含权限向导）
  7) 番茄钟（Pomodoro）
  8) 纪念日/倒计时（Anniversary）
- 规则引擎（轻量）：阈值触发 + 时间段触发；动作：变色/角标；写入告警中心。
- 告警中心（本地闭环）：触发历史、详情快照、去重频控、通知通道开关（默认关闭）。
- Footer 健康度（强制统一）：最后更新时间 + 状态（正常/断网/限流/Key 缺失/Key 失效/权限缺失/已暂停）+ 对应修复动作。
- 全局刷新调度器：请求合并、可见优先、后台降频、失败指数退避、一键暂停全部刷新。
- Key 管理与向导：按 Provider 分类、试请求校验、失效原因可理解、便签内一键修复入口。
- 权限向导（必须）：日历权限“预热页 → 再弹系统授权”；拒绝后提供明确系统设置指引。

### 3.2 v1.1（P1）
- RSS 情报站模板：只做标题/短摘要/时间/来源，点击外跳浏览器；默认刷新 30 分钟；按域名合并任务、严禁并发。
- “大宗商品/外汇”通用行情模板：复用 Market UI/规则/趋势线逻辑（assetType 扩展）。

### 3.3 v2.0（P2）
- 物流、行程卡：延后（接口/解析不确定性高、隐私与售后风险高）。

---

## 4) 核心交互与入口（P0）
- 菜单栏入口：新建便签、搜索/切换便签、告警中心、暂停/恢复全部刷新、Key 管理、设置。
- 全局热键：新建便签、搜索便签、循环切换便签。
- 新建便签：2 步内完成（选模板 → 填最小配置 → 创建）。

---

## 5) 窗口与尺寸规范（P0）
### 5.1 严格尺寸（除 Note/会议记录外全部）
尺寸档位（统一）：
- S：240×160
- M：320×220（默认）
- L：420×300
- XL：560×380

约束：
- 禁止拖拽缩放（resizable=false）。
- 只允许通过「…」菜单或设置面板切换 S/M/L/XL（当前档位打勾）。
- 切换尺寸的锚点策略全局一致（建议：窗口左上角为锚点），并持久化。

### 5.2 自由缩放（Note/会议记录）
- 最小尺寸：不小于 M（320×220）。
- 仍可提供“吸附档位”手感（可选，不强制）。

---

## 6) 告警体系（P0）
- 触发器：
  - 阈值：字段 > / < 阈值（价格、涨跌幅、ETA、降雨概率、倒计时阈值等）
  - 时间段：在指定时段提高刷新档位或启用某规则
- 动作：
  - 便签变色（边框/背景）
  - 角标
  - 写入告警中心（快照必须包含触发值、阈值、时间戳、Provider、便签ID）
- 去重频控：
  - 默认 dedupeWindow=10 分钟（可配 5/10/30/60）
  - 同 noteId+ruleId 窗口期内只生成 1 条主告警，其余计入“合并次数”
- 通知通道：
  - 系统通知：默认关闭
  - 声音：默认关闭（静音默认）
  - 仅用户显式开启后才允许发送

---

## 7) 数据源 Provider 架构（P0）
统一抽象（产品要求）：
- fetch(config) → 结构化数据 + dataTimestamp
- healthCheck(key/config) → OK / INVALID_KEY / RATE_LIMIT / OFFLINE / UNKNOWN
- rateLimitHint → 建议刷新频率与最大频率

要求：
- TrafficProvider、WeatherProvider 必须可切换 Provider。
- Provider 切换必须试请求；失败需回滚并给出可理解原因。
- 失败时必须保留并展示“上次成功快照 + 时间戳”，禁止空白。

---

## 8) 模板规格（v1.0 关键点）

### 8.1 币价（Market）— 含趋势线（必做）
- 严格尺寸：S/M/L/XL
- 趋势线策略：
  - 仅 L/XL 显示 Sparkline
  - 固定窗口：近 60 分钟
  - 采样间隔 = 刷新频率（1/5/15/60min）
  - 数据来源：只使用轮询返回的 price，本地环形缓存绘制；禁止为趋势线额外发请求
  - 不可见时暂停重绘（降 CPU）
- 技术指标：v1.x 不做 MA/RSI/MACD
- 规则示例：涨跌幅阈值、价格突破阈值

### 8.2 股票（Stock）
- 严格尺寸：S/M/L/XL（v1.0 可先 S/M 主用，L/XL 信息增强）
- 刷新策略：盘中高频、盘后低频（预设档）

### 8.3 交通（Traffic）
- 严格尺寸：默认 L（信息更适配），最小 M
- 配置：起点/终点（定位或手动），Provider 可切换
- 规则示例：ETA > X 分钟变色
- 定位权限：拒绝时可用手动地点；提供授权入口

### 8.4 天气（Weather）
- 严格尺寸：默认 M，最小 S
- 规则示例：降雨概率 > X% 变色

### 8.5 记事（Note）+ 会议记录（Note 变体）
- 自由缩放（最小 M）
- AI 一键整理：输出标题/要点/待办；必须保留原文，可一键回滚
- 离线 100% 可用（AI 若不可用需降级提示，不影响编辑）

### 8.6 日历/会议倒计时（Calendar）
- 严格尺寸：默认 S/M
- 权限向导（必须）：先“权限预热页”，用户确认后再弹系统授权
- 展示：下一场会议标题 + 开始时间 + T-倒计时；可选 Join（若有链接）
- 规则：会前 10 分钟变黄，会前 1 分钟变红（默认不通知）

### 8.7 番茄钟（Pomodoro）
- 严格尺寸：默认 S
- 动作：开始/暂停/重置
- 结束提醒：默认仅变色/角标；通知/声音用户手动开启
- 状态持久化建议：重启后“自动暂停并保留剩余时间”（防误响/防漂移）

### 8.8 纪念日/倒计时（Anniversary）
- 严格尺寸：默认 S
- 规则：T-1 天变黄、T-1 小时变红（默认不通知）

---

## 9) RSS 情报站（P1 约束提前写死，避免膨胀）
- 默认刷新：30 分钟
- 多 RSS 便签：按域名合并为单任务队列，严禁并发
- 展示：标题/来源/时间/短摘要；点击外跳浏览器
- 不做：站内全文阅读器、复杂排版与离线全文

---

## 10) 非功能性硬指标（继承并补强）
- 性能：10 个便签同时运行，内存占用 ≤ 150MB；20 张常驻不卡顿（请求合并/降载生效）。
- 容错：任何 API 报错（401/429/500），Footer 必须显示人能看懂的中文提示，禁止直接甩代码。
- 交互：所有设置操作 2 步内触达（齿轮 → 改参数）。
- 离线：记事、番茄钟、日历（缓存/本地）100% 可用。
- 省电：不可见降载、失败指数退避，避免高频重试风暴。

---

## 11) 原型交付验收（P0，必须点得动）
- 便签齿轮设置可点、Tab 可切；“…”菜单可点（置顶/锁定有状态）。
- Footer 失败态（缺 Key/Key 失效/限流/断网/权限缺失）均可点击进入修复链路，并能回到正常态页面。
- 严格尺寸模板：无 resize handle；可通过菜单切 S/M/L/XL 跳转对应 Frame。
- Note：保留 resize handle，并展示至少两种缩放状态。
- 告警闭环：便签角标 → 告警中心 → 详情 → 跳回便签。

---

## 12) 初步验收标准（v1.0）
1. 新建：任意 P0 模板 2 步内创建成功；网络正常且 Key 正确时 ≤10 秒出首屏数据。
2. 趋势线：Market 在 L/XL 必显示近 60m Sparkline；趋势线不增加网络请求（仅用轮询点）。
3. 失败可见：断网/限流/Key 问题/权限缺失均有明确提示 + 修复入口；联网模板断网不空白，必须显示上次快照。
4. 告警：触发变色/角标 + 告警中心有快照；去重频控生效；默认不发系统通知。
5. 尺寸策略：除 Note/会议记录外全部严格 S/M/L/XL；Note 可自由缩放且不破版。
6. 性能：20 张便签常驻稳定；同标的/同源请求合并可验证；无持续 CPU 飙高。

---