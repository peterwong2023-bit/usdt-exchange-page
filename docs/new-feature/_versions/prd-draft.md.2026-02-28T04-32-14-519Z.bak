# 币价模板（Market）细化规格 v1.1（严格尺寸 + 趋势线必做）

## 1. 模板定位
- 核心价值：桌面“一眼看”当前价与涨跌；规则触发变色/角标；失败可见（不允许静默停更）。
- 约束：严格尺寸（不可拖拽缩放），仅允许 S/M/L/XL 四档切换；默认不发系统通知/不播声音。
- v1.1 变更：L/XL 的趋势线（Sparkline）改为“必做”，且不允许引入额外高频接口。

[便签] 趋势线必做但不加网络成本：只用现有轮询返回的 price 追加本地环形缓存绘制 sparkline；不可见时暂停重绘降 CPU。

## 2. 尺寸档位与布局（S/M/L/XL 4 套）
尺寸：S 240×160；M 320×220（默认）；L 420×300；XL 560×380  
通用：Header 32、Footer 24 固定；Body 自适应；Footer 永不被挤没。

### 2.1 Header（所有尺寸一致）
- 左：资产图标（币种）+ 标题（默认“BTC/USDT”或“BTC/USD”，可改名）
- 右：刷新按钮 + 齿轮（设置）+ “…”（更多菜单）
- 更多菜单至少包含：尺寸（S/M/L/XL）、置顶、锁定位置、透明度、复制、关闭

### 2.2 Footer（所有尺寸一致，强制）
- 左：最后更新（例：“2 分钟前”）
- 中：状态 Chip（正常/断网/限流/Key 缺失/Key 失效/已暂停）
- 右：动作按钮（按状态变化）
  - 正常：可不显示按钮或显示“查看告警”(如有未查看)
  - 断网/限流：重试 / 查看详情
  - Key 缺失/失效：修复 Key
  - 已暂停：恢复

### 2.3 Body（按尺寸显示的信息档位）
#### S（240×160）= “1 主 + 1 副”
- 主指标：现价（大字）
- 副指标：24h 涨跌幅（%）+ 红绿
- 不显示：高低点、成交量、趋势线

#### M（320×220）= “补 1-2 个关键字段”
- 主指标：现价（大字）
- 副指标：24h 涨跌额 + 24h 涨跌幅（同一行，红绿）
- 辅助：标的/计价（如 BTC/USDT）+ 小号“来源/交易所”(可选一行)
- 不显示：趋势线

#### L（420×300）= “加趋势（必做）+ 扩展信息”
- 主指标：现价（大字）
- 副指标：24h 涨跌额 + 涨跌幅
- 辅助：24h 高/低（两列）
- 趋势线（必做）：Sparkline（最近 T 时间窗口，默认 60 分钟），仅展示不交互

#### XL（560×380）= “趋势增强（必做）”
- L 的全部
- 趋势线增强（必做其一，v1.0/v1.1 选 A）：
  - A：更宽的 Sparkline + 标注当前点（圆点）+ 右侧显示窗口标签“近 60m”
  - B：在趋势线下增加极简刻度（仅起点/终点时间，非完整坐标轴）
- 禁止：多周期切换按钮、缩放拖拽、K 线等复杂交互（不在 v1.x）

## 3. 数据字段定义（内部数据模型）
MarketQuote（最小结构）
- symbol：BTC
- quote：USDT（或 USD/CNY）
- price：当前价（number）
- change24hAbs：24h 涨跌额（number）
- change24hPct：24h 涨跌幅（number，-0.052 = -5.2%）
- high24h / low24h（L/XL 用，optional）
- volume24h（optional，v1.x 可不展示）
- dataTimestamp：数据时间戳（provider 返回）
- fetchTimestamp：拉取时间戳（本地）
- providerId：数据源标识
- currencySuffix：单位后缀（可配置，如“USDT”“$”“￥”）

### 3.1 Sparkline 本地缓存结构（新增，必需）
MarketSparklinePoint
- t：时间戳（ms）
- p：price（number）

缓存规则
- 每次成功拉取 quote 时，将 (fetchTimestamp, price) 追加到本地环形缓存
- 缓存窗口：默认保留最近 6 小时点；绘制时取最近 60 分钟子集
- 最大点数上限：按刷新档位估算并封顶，例如：
  - 1min 刷新：最多 360 点（6h）
  - 5min 刷新：最多 72 点（6h）
  - 15min：最多 24 点（6h）
  - 60min：最多 12 点（12h 可选）
- 禁止：为了趋势线额外发请求；趋势线只吃“已有轮询结果”

## 4. 数值格式规则（统一口径）
- price：
  - ≥1000：保留 2 位小数
  - 1~999.99：保留 2~4 位（按币种小数精度上限配置，默认 2）
  - <1：最多 6 位小数（建议四舍五入）
- change24hPct：显示 2 位（例：-5.23%）
- 红绿：涨=绿，跌=红；0 为中性灰

## 5. 设置项（Popover：Market 专属 + 通用）
### 5.1 概览
- 当前标的：BTC/USDT
- Provider：Binance/OKX/聚合源（示例）
- 最近成功时间：展示 dataTimestamp 与 fetchTimestamp
- 趋势线窗口（只读）：近 60m（v1.x 固定，不给用户改，避免复杂度）

### 5.2 刷新
- 刷新档位：1/5/15/60 分钟
- 可见优先：默认开
- 后台降频：默认开
-（只读）失败退避说明：指数退避

### 5.3 规则（v1.x 必须可用，轻量）
规则类型（至少两种）
1) 价格阈值：price > X 或 < X
2) 涨跌幅阈值：abs(change24hPct) > X%
动作（v1.x）
- 变色（背景/边框）+ 角标
- 写入告警中心（含快照）
去重
- dedupeWindow 默认 10 分钟，可改（5/10/30/60）

### 5.4 通知通道（默认全关）
- 系统通知：Off
- 声音：Off
- 说明：仅开启后且未被去重时发送

### 5.5 数据源 & Key
- Provider 下拉选择
- 该便签绑定的 Key（选择/默认 Key）
- Key 状态：有效/缺失/失效/配额不足
- 测试连接（试请求）
- 修复 Key（跳 Key 向导）

## 6. 交互流程（原型必须点通）
### 6.1 新建币价便签（2 步）
菜单栏“+”→ 选择“币价”→ 输入/搜索 BTC → 选择计价货币（默认 USDT）→ 创建

### 6.2 Key 缺失/失效修复闭环（P0）
Footer=Key 缺失/失效 → 点“修复 Key”→ Key 向导粘贴 → 试请求成功 → 自动刷新 → Footer=正常（并恢复趋势线绘制）

### 6.3 严格尺寸切换
“…”菜单：尺寸 → S/M/L/XL（打勾）  
切换锚点：统一采用窗口左上角（或中心点二选一，开发全局一致）  
resizable=false（禁止拖拽缩放）

## 7. 失败态与文案（统一且趋势线有降级）
- 断网（Offline）
  - Body：显示上次成功快照（价格/涨跌仍显示）
  - 趋势线：显示上次已缓存趋势线，但整体置灰 + 提示“离线，展示 X 分钟前数据”
  - Footer：状态=断网；动作=重试
- 限流（429/RateLimit）
  - Body：显示上次快照；提示“请求过于频繁，已自动放慢刷新”
  - 趋势线：继续显示历史缓存；不追加新点
  - Footer：状态=限流；动作=查看详情/切换 Provider
- Key 缺失
  - Body：显示占位（—）+ 提示“需要 API Key 才能拉取数据”
  - 趋势线：不显示（或显示空态占位线）
  - Footer：状态=Key 缺失；动作=修复 Key
- Key 失效（401）
  - Body：显示上次快照（若有）+ 提示“Key 无效或已过期”
  - 趋势线：显示历史缓存置灰（若有）
  - Footer：状态=Key 失效；动作=修复 Key

## 8. 性能与缓存（硬口径）
- 请求合并：同 providerId + symbol+quote + 相同刷新档位合并为 1 次请求
- Sparkline 绘制约束：
  - 不新增网络请求
  - 视图点数上限：按实际像素宽度下采样（例如每 2-3px 取 1 点），避免 1:1 绘制导致 CPU 抖动
  - 不可见降载：窗口不可见/被遮挡时暂停重绘（缓存仍可追加点）
- 缓存清理：便签删除时清除其 sparkline 缓存；可选全局“清空缓存”入口（P1）

## 9. 初步验收标准（币价模板专属）
1) L/XL 趋势线必显示：创建后在 2 次成功刷新内出现 sparkline；切换 L↔XL 不闪烁、不重置历史  
2) 趋势线不加请求：抓包/日志验证趋势线无额外 API 调用，仅使用轮询结果  
3) 失败降级正确：断网/限流时仍显示上次趋势线（置灰），并显示上次成功时间  
4) 严格尺寸：不可拖拽缩放；仅 S/M/L/XL 切换且位置锚点不乱跳  
5) 性能：10 张币价便签（其中 5 张 L/XL 开趋势线）常驻不卡顿，CPU 不持续飙高（短时绘制可接受）

下一步需要你拍板 1 个细节（影响开发量不大，但要统一）：趋势线窗口固定“近 60 分钟”是否同意？（建议同意，v1.x 不开放多周期切换）