# Market 多币种看板 - 功能说明文档

## 功能概述
本功能为桌面常驻的轻量化行情监控看板，支持多币种实时价格列表（Watchlist）及 K 线详情查看。核心逻辑在于通过严格的窗口尺寸控制（S/M/L/XL）来平衡信息密度与交互体验，确保在不同桌面占用空间下均能提供有效的决策支持。

## 术语定义
| 术语 | 定义 |
| :--- | :--- |
| **Watchlist** | 用户自定义的币种监控列表，显示交易对、现价及 24h 涨跌幅。 |
| **S/M Gate** | 尺寸拦截机制。当窗口处于 S 或 M 码时，限制进入详情页，引导用户扩充尺寸。 |
| **失败快照** | 当网络断开或 API 限流时，界面不显示空白，而是显示最后一次成功获取的数据并置灰。 |
| **Ticker 批量请求** | 将列表所有币种的行情请求合并为一个接口调用，降低功耗。 |

## 业务流程图
```mermaid
flowchart TD
    A[启动 Market 模板] --> B{当前窗口尺寸}
    B -- S/M --> C[展示 Watchlist 列表]
    B -- L/XL --> D[展示 Watchlist 列表]
    C --> E{点击币种行}
    D --> F{点击币种行}
    E --> G[弹出 S/M Gate 引导层]
    G -- 点击取消 --> C
    G -- 点击切换 L/XL --> H[调整窗口尺寸并进入详情页]
    F --> I[直接进入详情页]
    I --> J{点击返回}
    J --> D
```

## 数据流图
```mermaid
sequence diagram
    participant U as 用户
    participant F as 前端界面
    participant S as 后端/API
    U->>F: 打开/可见窗口
    F->>S: 发送 Ticker 批量请求 (List)
    S-->>F: 返回实时价格/涨跌幅
    F->>F: 渲染 Watchlist
    U->>F: 点击币种 (L/XL 尺寸)
    F->>S: 停止 Ticker 请求，发起 OHLC 请求 (5m)
    S-->>F: 返回 K 线数据
    F->>F: 渲染 K 线图表
    U->>F: 退出详情页
    F->>S: 停止 OHLC 请求，恢复 Ticker 批量请求
```

## 功能详述

### 模块 1：Watchlist 列表页
- **描述**：多币种行情监控主界面。
- **交互说明**：
  - 列表行数随窗口尺寸固定：S(2行)、M(4行)、L(6行)、XL(10行)。
  - 点击“+”号进入币种搜索添加页。
  - 右键币种行弹出“删除”菜单。
- **输入**：用户搜索的交易对名称（如 BTC/USDT）。
- **输出**：交易对名称、实时价格（动态跳动）、24h 涨跌幅（红/绿颜色区分）。
- **异常处理**：
  - 网络中断 → 价格文字变灰色，显示“数据更新于 X 分钟前”小字，保留最后一次数值。
  - API 限流 → 触发指数退避算法，降低刷新频率，状态栏显示警告图标。
- **权限要求**：无。

### 模块 2：S/M Gate 引导层
- **描述**：小尺寸下的交互拦截器。
- **交互说明**：
  - 以半透明遮罩弹窗形式出现。
  - 提供“切换到 L 查看”、“切换到 XL 查看”、“取消”三个操作项。
- **输出**：点击切换后，窗口物理尺寸改变，界面同步切换至详情页。

### 模块 3：K 线详情页（仅 L/XL）
- **描述**：单个币种的深度行情查看。
- **交互说明**：
  - 默认展示 5m 周期 K 线。
  - 顶部提供 1m/5m/15m/1h 切换按钮。
  - 点击左上角“返回”图标回到列表页。
- **输入**：周期切换指令。
- **输出**：OHLC 蜡烛图、当前价、最高/最低价标注。
- **性能约束**：**必须**在退出详情页时立即销毁 K 线轮询请求，禁止后台静默拉取。

## 状态机
```mermaid
stateDiagram-v2
    [*] --> List_Normal: 初始化
    List_Normal --> List_Offline: 网络断开/请求失败
    List_Offline --> List_Normal: 网络恢复/手动修复
    List_Normal --> Detail_View: 点击币种 (L/XL)
    Detail_View --> List_Normal: 点击返回
    Detail_View --> Detail_Offline: 详情页请求失败
    Detail_Offline --> Detail_View: 恢复连接
```

## 接口需求概要
| 接口名称 | 方法 | 路径 | 主要参数 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 批量行情接口 | GET | /api/v1/ticker/batch | symbols=[...] | 获取列表所有币种实时数据 |
| K 线数据接口 | GET | /api/v1/klines | symbol, interval | 获取指定币种 OHLC 数据 |
| 币种搜索接口 | GET | /api/v1/search | keyword | 模糊匹配交易对 |

## 数据模型
| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| symbol | String | 是 | 交易对名称，如 "BTCUSDT" |
| lastPrice | Number | 是 | 最新成交价 |
| priceChangePercent | Number | 是 | 24小时涨跌幅百分比 |
| klineData | Array | 是 | 包含 [时间, 开, 高, 低, 收] 的数组 |
| lastUpdateTime | Timestamp | 是 | 最后一次成功请求的时间戳 |

## 非功能需求
- **性能要求**：列表页 Ticker 刷新频率 3s/次，详情页 OHLC 刷新频率 5s/次。
- **安全要求**：支持脱敏模式，开启后隐藏具体价格数值，仅保留涨跌颜色。
- **兼容性**：需适配 macOS/Windows 窗口透明度设置，确保 K 线图在半透明背景下的可读性。

[便签] 交互核心：S/M 尺寸点击行必须弹 Gate 拦截，严禁直接在小窗渲染 K 线。
[便签：性能风险] 退出详情页必须硬性停止 OHLC 请求，防止多窗口后台堆积导致限流。