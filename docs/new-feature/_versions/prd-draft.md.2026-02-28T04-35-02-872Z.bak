## 1) 你说的“1分钟、5分钟那种”——有两层含义
### A. 采样间隔（实际点的间隔）
- 有：点的间隔=你的刷新频率  
  - 刷新 1min → 趋势线约每 1 分钟一个点  
  - 刷新 5min → 趋势线约每 5 分钟一个点  
- 窗口仍是“近 60m”，只是点更稀疏/更平滑。

### B. 多周期切换（1m/5m/15m/1h 视图按钮）
- v1.x 不做：这会引入额外交互与状态复杂度，也容易被用户理解为“K线级能力”。

结论：**有“采样间隔”，没有“多周期切换按钮”。**

## 2) 技术指标（MA/RSI/MACD）——v1.0 不做
- v1.0/v1.1：不做任何技术指标，只画价格 sparkline +（XL）当前点标注。
- 原因（产品+技术都不划算）：
  1) 我们只有轮询 price 点，不是 OHLC K 线；指标会“看起来专业但不可靠”
  2) 用户会要求更多周期/更多参数，范围失控
  3) 计算+绘制叠加会增加 CPU（多便签场景风险高）

## 3) 如果你坚持要“有一点指标味道”，最低成本方案（P1 备选，不加请求）
只给一个“辅助线”，不做完整指标面板：
- 方案：**SMA/EMA 一条均线（比如 10 点均线）**
- 约束：
  - 仅在 XL 显示
  - 不开放参数配置（写死 10 点）
  - 明示：基于本地采样点计算（避免用户误解为交易指标）

## 4) 需要你拍板两件事（决定趋势线的最终形态）
1) 趋势线窗口是否固定“近 60m”（建议固定）  
2) 是否允许在 XL 额外叠加“一条均线”（默认建议不做，放 P1）

你选：  
- A 固定 60m + 不加指标（最稳）  
- B 固定 60m + XL 加 1 条均线（P1）