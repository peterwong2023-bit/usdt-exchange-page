## 仍需补齐/拍板的 8 个点（不补会画两套）
1) 列表页 Mini Trend：S 是否画、M 是否画（你文档里写“可选”，会导致两套布局）  
2) K 线默认周期：1m 还是 5m（你留了待定；会影响默认跳转帧与按钮高亮）  
3) 详情页承载方式：点击 Row 后是“同窗口切到详情态”还是“新开详情窗口”（原型连线不同）  
4) 详情页控件布局固定：周期按钮位置（顶部右侧/图上方一行），指标入口是否 v1.0 就露出（现在是“可选P1”，需明确原型是否画出来）  
5) Watchlist 管理：v1.0 是否需要“添加币种”入口（空列表怎么办）、是否需要“删除/置顶”（你写可选，会影响是否要画菜单）  
6) 列表行字段口径：是否始终显示 Pair（USDT/USD），还是仅 Symbol（BTC），以及价格精度/单位（否则设计会随意截断）  
7) L/XL 进入详情是否允许从列表直接点（是），那返回按钮放哪里（左上返回 vs 底部返回）需固定一种  
8) 失败态的“快照灰显规则”：断网/限流时列表数字是否置灰、详情 K 线是否置灰并显示“更新于 X 分钟前”（需要一句话写死）

## 建议你立刻定死的 2 个（对原型影响最大）
- Mini Trend：S 不画、M 不画（最克制、最省性能；与既有定稿口径一致）或 S 不画、M 画（折中）二选一  
- 默认周期：5m（更稳、也更符合“轻量盯盘”）

## 原型能否评审通过的最低门槛（按你文档可直接验）
- 任意尺寸列表页可切 S/M/L/XL  
- S/M 点 Row 必出 Gate，并能一键切到 L/XL 再进详情  
- L/XL 点 Row 直进详情，周期按钮能切 1m/5m/15m/1h，能返回列表  
- 列表/详情失败态都能从 Footer 点到“修复 Key/重试/切 Provider”并回正常态

你给出上述第 1、2 点的最终选择（Mini Trend 是否画；默认周期 1m/5m），我就可以把 v1.3 文稿补成“无可选项版”，设计按 Frame 清单直接开画不返工。