## 1) 尺寸档位定义（建议像素）
以 macOS 常见密度为基准（点=像素处理也行，原型先用 px）：

- 小 S：240 × 160（接近最小可读）
- 中 M：320 × 220（默认）
- 大 L：420 × 300（信息增强）
- 最大 XL：560 × 380（接近“看板卡”）

通用规则
- **最小尺寸**：不小于 240×160（低于就内容必炸）
- Header/Footer 固定高度（比如 32/24），Body 自适应
- 拖拽缩放时：靠近 S/M/L/XL 尺寸自动吸附；按住 Option 可临时取消吸附（可选）

## 2) “尺寸档位 = 信息档位”（同一模板不同尺寸显示不同信息）
这样做的好处：模板不需要为无限尺寸做排版；只要保证 4 套布局。

### 模板与推荐默认尺寸（以及各尺寸显示内容）
| 模板 | 默认档位 | S（小）展示 | M（中）展示 | L（大）展示 | XL（最大）展示 |
|---|---|---|---|---|---|
| 币价 Market | M | 现价 + 涨跌% | + 24h高低 | + 迷你趋势/成交量（可选） | + 多周期切换/更大趋势（后续） |
| 股票 Stock | M | 现价 + 涨跌% | + 昨收/今开（可选） | + 成交量/盘中状态 | + 小图/更多字段（后续） |
| 交通 Traffic | M/L（看路况更需要高度） | ETA + 拥堵条 | + 路线摘要 | + 备选路线1条（若源支持） | + 出发建议/更多路线（后续） |
| 天气 Weather | M | 温度 + 降雨% | + 体感/风（可选） | + 未来3小时条（后续） | + 今日/明日切换（后续） |
| 记事 Note | L（更像编辑器） | 只读摘要/几行文本 | 基础编辑 | 更舒适编辑区 + 工具按钮 | 更接近小编辑器（但仍不做全文编辑器） |
| 日历 Calendar | S/M（倒计时本质一眼看） | 下一场标题（截断）+ T- | + 开始时间/Join | + 今日后续1-2场（可选） | + 日程列表（不建议 v1.0 做太满） |
| 番茄钟 Pomodoro | S（核心就一个大数字） | 大倒计时 + 状态 | + 开始/暂停/重置 | + 进度环/本轮统计（可选） | + 历史/徽章（P1） |
| 纪念日 Anniversary | S | D-天大字 | + 目标日期/剩余小时（可选） | + 备注/重复设置入口（可选） | + 列表化多事件（若未来做“纪念日集合卡”） |

## 3) 关键实现约束（避免研发扯皮）
- 每个模板必须定义：**默认档位 + 最小可用档位**  
  例如：Pomodoro/Anniversary/Calendar 最小就能用；Traffic/Note 最小可能会难看，默认给 M/L。
- 小档位下：禁止塞“设置项/二级信息”，只保留 1 个主指标 + 1 个副指标。
- 任何档位都不能让 Footer 被挤没（健康度必须可见）。

## 4) 需要你拍板的 1 个决策
- 尺寸控制策略选哪种：
  - A）**档位 + 仍可自由缩放**（推荐：兼容所有人）
  - B）严格只能选 S/M/L/XL（更规整，但会被吐槽“不够像便签能随便拉”）

你定 A 或 B，我再把每个模板的 S/M/L/XL 具体排版草图（字段排列、截断规则、是否显示趋势/列表）补成可直接给原型师画的规格。