# AI 桌面便签（AI Sticky）- PRD 草案（PM-Alpha）

## 需求背景
用户在桌面常驻信息的需求长期存在（mac 便签/小组件/菜单栏工具），但现有产品多是“静态内容承载”。用户真正想要的是“桌面上贴着的不是字条，而是会自己更新、会回答问题、会联动数据源的活便签”：币价/股价/交通/天气/待办/笔记等信息需要实时、按用户上下文自动刷新；同时仍保持“拖到桌面、随手瞄一眼”的低打扰体验。  
目标形态：macOS 可安装应用（dmg），桌面可独立贴多个便签窗口，每个便签可配置为不同 AI 能力与数据源，支持自定义扩展。

## 目标用户
- 投资与信息跟踪用户：关注币价/股票/宏观新闻，想要桌面低成本监控与摘要。
- 通勤与出行用户：需要当前位置交通、路线拥堵、ETA 变化提醒。
- 生活效率用户：天气/日程/待办/备忘，要求快速记录与可检索。
- 轻量办公用户：会议纪要临记、灵感捕捉、跨应用复制粘贴，要求“随手写、自动整理”。

## 创新灵感（碰撞区思维 + 反转练习的发现）
- 碰撞区思维：把“便签”当成“乐高积木”。每张便签不是固定功能，而是由“数据源积木 + 展示积木 + AI 规则积木”拼装出来；同一种数据源可以被不同展示方式复用（列表/图表/一句话摘要/红绿灯状态）。
- 把“数据管理”当成“水流”。便签不是拉取一次就结束，而是持续流式更新：定时刷新、阈值触发、上下文触发（位置变化/网络变化/时间段）。
- 反转练习：  
  - 常规：用户每次打开看信息 → 反转：信息在桌面“自己变对”。用阈值/异常驱动，只在“值得注意”时变醒目（颜色/角标/轻提示）。  
  - 常规：便签只是内容 → 反转：便签是“可提问的视图”。用户可直接对某张便签提问（“这波上涨原因？”“今晚 8 点出发堵吗？”），答案只引用该便签的数据源，减少幻觉。  
  - 常规：自定义要写很多配置 → 反转：用“自然语言生成便签”。用户说“做一个跟踪特斯拉的便签，跌 3% 变红并提示原因”，系统自动组装模块并给出可编辑规则。

---

## 方案对比

### 方案 A：模板化 AI 便签（行业标准基准）
- 核心功能点
  - 预置便签模板：币价、股票、天气、交通、记事、待办。
  - 便签窗口：可拖拽、置顶、透明度、尺寸、颜色主题、自动吸附边缘。
  - 定时刷新：每 1/5/15/60 分钟可选；手动刷新。
  - 基础 AI：对当前便签内容做摘要/解释（如币价波动摘要、天气出行建议）。
  - 基础设置：数据源 API Key 管理、位置权限、单位与时区。
- 用户故事
  - 作为用户，我从模板库添加“币价便签”，选择 BTC/ETH，桌面常驻显示价格与 24h 涨跌。
  - 作为用户，我添加“交通便签”，授权定位后显示到公司通勤 ETA 与拥堵状态。
- 创新亮点
  - 上手快、路径最短；模板覆盖核心场景。
- 潜在风险
  - 扩展性弱：用户想要“自定义指标/自定义规则/自定义数据源”会卡住。
  - AI 容易沦为“附加按钮”，而非工作流核心。
- 极端尺度测试结果
  - 体量（1 张 vs 100 张便签）：100 张定时刷新会造成 CPU/网络压力，需要全局调度与刷新合并，否则电量暴跌。
  - 速度（瞬时 vs 1 年运行）：长期运行会遇到内存泄漏、API 配额、token 费用不可控；需限流与缓存。
  - 用户（1 人 vs 100 万人）：服务端若统一代理数据源会有成本爆炸；模板方案更适合“客户端直连 API”但配置门槛升高。
  - 故障率（永不故障 vs 总是故障）：任一数据源挂了就出现空白，需要离线占位与降级文案。

---

### 方案 B：模块化“便签积木” + 规则引擎（创新优化方案）
- 核心功能点
  - 便签 = 数据源模块 + 展示模块 + AI 解释模块 + 规则模块（可视化编辑）。
  - 数据源模块（首批）
    - 行情：加密货币（交易所/聚合）、股票（报价/基本面/新闻）、指数与汇率（可选）。
    - 位置：系统定位 + 手动地点；交通：Google Maps（或可替代数据源）。
    - 天气：按定位/城市；支持未来 24h/7 天。
    - 笔记：本地富文本/Markdown；可选 iCloud 同步（后续）。
  - 展示模块
    - 迷你模式（一行）、卡片模式（关键指标）、图表模式（迷你 K 线/趋势线）、列表模式（新闻/事件）。
  - 规则引擎（便签级）
    - 阈值触发：涨跌幅、温度、降雨概率、ETA 超阈值。
    - 时间触发：工作日早高峰/收盘前。
    - 上下文触发：位置变化、网络从 Wi‑Fi 切换到蜂窝（若未来扩展到移动端）。
    - 动作：高亮变色/角标/轻提示/生成一句话解释/记录到日志。
  - AI 交互（便签内 Chat / 指令）
    - “只基于本便签数据源回答”：减少幻觉；引用来源与时间戳。
    - 便签生成器：用自然语言创建便签（自动挑模块+默认规则），并给出可编辑配置。
  - 成本与性能
    - 全局刷新调度器：合并请求、分级刷新（前台高频/后台低频）、缓存与断网策略。
    - 本地事件日志：记录触发原因，便于复盘与调参。
- 用户故事
  - 作为用户，我创建“BTC 便签”：展示迷你 K 线；当 1h 波动 >2% 自动变红/变绿，并生成一句话原因（结合新闻标题与链上/市场指标的可用项）。
  - 作为用户，我创建“通勤便签”：每天 17:30 开始高频刷新；ETA 超过 45 分钟时提示并给出替代路线建议。
  - 作为用户，我创建“天气便签”：明天降雨概率 >60% 时提醒带伞，并在早晨 8 点自动弹出轻提示。
  - 作为用户，我有一张“记事便签”，输入杂乱文字后点击“整理”，AI 自动生成结构化要点与待办。
- 创新亮点
  - “便签积木化”让自定义成本极低，既能满足大众模板，也能满足重度用户自定义。
  - “规则驱动”把便签从被动展示变成主动提醒，但仍保持低打扰（只在异常时醒目）。
  - “便签内 AI 只回答本源数据”——把可信度当成产品卖点。
- 潜在风险
  - 配置复杂：模块与规则可能让小白困惑，需要“新手仅模板、进阶再解锁积木”的渐进式引导。
  - 数据源合规与限制：Google Maps/行情/新闻 API 授权与配额，需可插拔替代方案。
  - AI 成本：频繁生成“原因解释”可能昂贵，需要触发阈值+缓存+摘要复用。
- 极端尺度测试结果
  - 体量：10 亿条数据不现实，但“100 张便签 * 多数据源 * 高频刷新”是真实极端；必须有全局调度、后台降频、请求去重、按需渲染（不可见时暂停动画/图表）。
  - 速度：交通/行情要求秒级；天气/新闻分钟级即可。系统需按模块定义 QoS，避免“一刀切刷新”拖垮性能。
  - 用户：1 人本地运行 OK；若做云同步/规则云端推送，需把“数据获取”尽可能留在客户端，服务端只做同步与配置，不做大规模代理拉取。
  - 故障率：数据源常态会失败（超时/配额/被墙/无定位）。需要：自动切换备选源、展示上次成功时间、离线缓存、以及“降级为静态便签 + 手动刷新”。

---

### 方案 C：自进化“活便签代理”（颠覆性方案）
- 核心功能点
  - 每张便签是一个小型 Agent：具备目标、工具、记忆与自我调参。
  - 便签会“自己变形”：根据用户行为学习展示方式（你常看涨跌幅→默认突出；你常问原因→预生成解释）。
  - 便签之间可协作：例如“币价便签”触发异常 → 通知“新闻便签”拉取相关主题 → 生成联动结论贴到“复盘便签”。
  - 便签市场：用户发布/订阅“便签配方”（包含模块+规则+提示词+工具链），一键安装。
- 用户故事
  - 作为用户，我说“我想盯盘但不想被打扰”，系统自动学习：只有当波动超过我的历史关注阈值才提示，并在我点击时给三条最可能原因与来源。
- 创新亮点
  - 便签从“工具”跃迁为“桌面上的个人信息系统”，强差异化。
- 潜在风险
  - 可控性与信任：Agent 自作主张会让用户不安，需要强可解释与可回滚。
  - 复杂度爆炸：调参、协作、市场生态都需要大量产品与工程投入，不符合 YAGNI 的早期策略。
  - 合规与安全：便签配方可执行工具链，存在恶意配置风险，需要沙箱与审核。
- 极端尺度测试结果
  - 体量：便签协作形成“风暴式调用”风险（一个触发导致多便签连锁反应），必须全局配额与熔断。
  - 速度：Agent 推理链会引入延迟，桌面便签“瞄一眼”的优势会被破坏，需要本地快照 + 异步补全。
  - 用户：市场规模起来后审核与安全成本陡增。
  - 故障率：任一工具不可用会导致 Agent 任务失败，需要严格的降级路径与最小可用展示。

---

## 推荐方案及理由
推荐方案 B（模块化“便签积木” + 规则引擎）。  
理由：
1. 差异化足够强：不是再做一个“模板便签”，而是把便签变成可拼装的信息卡系统，满足“币/股/交通/天气/记事 + 自定义”的真实诉求。
2. 投入产出比最优：相比方案 C 的 Agent 生态，B 的复杂度可控，且可渐进式演进到“半代理化”（先规则触发，再轻量联动）。
3. 可用性与可信度更容易做扎实：限定便签数据域回答、明确时间戳与来源、可解释触发原因，能建立长期信任。

---

## 用户故事（推荐方案详细版）
1. 创建与管理
   - 用户从“添加便签”面板选择模板（币价/股票/交通/天气/记事/自定义），生成一张可拖拽便签；支持复制、锁定、分组（如“投资”“出行”）。
2. 币价便签（A 便签）
   - 选择币种与交易所/聚合源；默认显示现价、24h 涨跌、迷你走势。
   - 设置规则：1h 波动 >2% 高亮；点击“解释”在便签内生成：波动摘要 + 相关消息标题（带时间）+ 风险提示（非投资建议）。
3. 股票便签（B 便签）
   - 输入代码/名称；显示现价、涨跌、成交量（或简化版）。
   - 规则：盘中跌破某价位提示；收盘后生成当日一句话复盘（可选）。
4. 交通便签（C 便签）
   - 授权定位或设定起终点；展示当前 ETA、拥堵等级、推荐出发时间窗。
   - 规则：ETA 超过阈值提示；“出发前 30 分钟”自动提高刷新频率；失败时展示上次成功与“手动重试”。
5. 天气便签（D 便签）
   - 按定位展示当前温度、体感、降雨概率、空气质量（可选）。
   - 规则：降雨概率 >60% 且在 7:00-9:00 触发提示；极端天气改用醒目配色。
6. 记事便签（E 便签）
   - 纯本地输入，支持快捷键快速呼出“快速记录条”写入指定便签。
   - AI 整理：将杂乱内容一键转换为要点/待办/会议纪要结构；支持“将待办同步到系统提醒事项（后续）”。
7. 自定义便签
   - 用户用自然语言描述：“做一个显示我关注的 3 个币 + 2 只股票的总览，波动大就变色”，系统生成便签并给出可编辑模块与规则。
   - 高级用户可手动选择数据源模块（行情/位置/天气/笔记）与展示模块（卡片/图表/列表），配置刷新频率与触发条件。

---

## 初步验收标准
1. 安装与运行
   - 提供 dmg 安装；首次启动引导完成；支持开机自启（可选）。
   - 便签窗口可在桌面自由拖拽、缩放、置顶/取消置顶、锁定位置；关闭可恢复（不丢配置）。
2. 核心便签可用（模板）
   - 至少交付：币价、股票、交通、天气、记事 5 类模板；每类模板可在 3 分钟内完成创建并看到数据。
3. 刷新与性能
   - 全局刷新调度生效：同一数据源多便签请求合并；后台降频；断网不崩溃。
   - 在 20 张便签常驻、其中 5 张高频刷新场景下：应用无明显卡顿；CPU/内存占用在可接受范围（以内部基准机型压测为准）。
4. 规则引擎（最小可用）
   - 支持至少 3 种触发：阈值触发、时间触发、手动触发；支持至少 2 种动作：高亮变色、轻提示。
   - 每次触发在便签内记录“触发原因 + 时间戳 + 数据快照”。
5. AI 交互可信度
   - 便签内“解释/问答”回答必须展示引用的数据源与更新时间；当数据缺失时必须明确告知“不足以判断”，不可编造。
6. 权限与失败降级
   - 定位未授权时：交通/天气便签提供手动地点输入；显示清晰的权限说明与替代路径。
   - API 失败/配额超限：展示上次成功时间、错误原因（可理解文案）、重试按钮与自动退避策略。