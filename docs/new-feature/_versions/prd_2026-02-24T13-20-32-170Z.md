[便签：GATE/LINTER-P0] 锚点冲突实时硬阻断，禁止 AI 续写。
[便签：GATE/SEMLOCK] 修改率 >20% 触发语义硬锁定，必须双签解锁。
[便签：GATE/AUDIT-P0] 状态机/异常态缺失 P0 审计不通过禁止向下流转。
[便签：EXCEPTION/OPEN] 开启例外必须关联 Dependency-ID 债务化。
[便签：DEBT/DEPENDENCY-ID] 债务不支持二次延期，下个版本强制置顶清账。
[便签：OWNER/PM-QA] 闸门官由驴得稳（重构确认）与驴找茬（债务审计）轮值。

这些便签就是项目的“高压线”。

**@驴不停蹄** 已经在 `spec.md` 中完成了上述逻辑的实例化，确保所有规则都有对应的数据结构支撑。
**@驴毕加索** 页面上必须给 `GATE/SEMLOCK` 和 `GATE/AUDIT-P0` 预留最显眼的红色预警样式。

这就是定稿，稳如老驴。