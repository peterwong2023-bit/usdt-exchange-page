# Market 多币种看板 + 币种详情 K 线 - PRD 定稿（v1.3-ProtoReady）

## 1. 需求背景
桌面常驻场景下，用户需要用最小干扰方式同时盯住多个币种价格波动（Watchlist），并在发现异常时快速进入单币详情查看走势（K 线）验证决策。现有方案要么只能看单币、要么终端过重、要么失败静默不可观测。

## 2. 目标用户
- 多持仓投资者：同时监控 BTC/ETH 及若干山寨币涨跌。
- 轻量交易者：用 5m/15m/1h K 线判断趋势，不做高频操作。
- 办公族极客：桌面常驻小窗，信息密度高、低打扰、可复盘。

## 3. 范围与非目标
### 3.1 v1.0（本稿覆盖）
- 一张 Market 便签：多币种列表（Watchlist）
- 币种详情：K 线（OHLC）+ 周期切换（1m/5m/15m/1h）
- 尺寸门槛：S/M 禁止看 K 线，必须 Gate 引导切换到 L/XL
- 数据策略：列表仅拉 Ticker；详情按需拉 OHLC；退出详情立即停止 OHLC
- 失败态：断网/限流/Key 问题必须展示“上次快照 + 更新时间 + 修复入口”，禁止空白

### 3.2 v1.0 不做
- S/M 列表不画任何 Mini Trend/迷你趋势图（列表页纯数值）
- 技术指标（RSI/MACD 等）不做；MA 也不做（避免范围与性能风险）
- 画线工具、深度图、交易下单
- 服务端代理代拉数据（坚持客户端直连 + 用户自备 Key）

## 4. 核心决策（写死，供原型/设计/研发统一）
1) 列表页 Mini Trend：S 不画、M 不画、L 不画、XL 不画（纯数值列表）  
2) K 线默认周期：5m  
3) 详情页承载方式：同一便签窗口内“列表态 ↔ 详情态”切换（不新开窗口）  
4) 周期按钮布局：详情页图表上方一行，右侧对齐（1m/5m/15m/1h），当前周期高亮  
5) 指标入口：v1.0 不露出（不画入口，不留按钮）  
6) Watchlist 管理：v1.0 必须支持“添加币种”与“删除币种”；不做置顶/拖拽排序（后续版本）  
7) 列表行字段口径：始终显示 Pair（如 BTC/USDT），避免仅 Symbol 歧义；价格精度采用“自适应精度规则”（见第 8 节）  
8) 失败态快照灰显规则：断网/限流时列表数值与详情 K 线整体置灰，并展示“更新于 X 分钟前”；图表交互禁用，仅保留返回/修复入口

[便签] 原型口径写死：列表页纯数值；K线默认 5m；S/M 点行必 Gate；L/XL 同窗进详情；失败态一律快照置灰+更新时间。

## 5. 尺寸与信息分级（严格尺寸）
### 5.1 尺寸档位
- S：240×160
- M：320×220（默认）
- L：420×300
- XL：560×380

约束：
- Market 便签为严格尺寸：禁止拖拽缩放（resizable=false）
- 只能通过「…」菜单或设置面板切换 S/M/L/XL；切换锚点固定（建议左上角），并持久化

### 5.2 列表展示行数（写死）
- S：2 行
- M：4 行
- L：6 行
- XL：10 行

## 6. 页面与交互（原型必须点通）
### 6.1 列表态（Watchlist）
#### 每行字段
- 左：Pair（BTC/USDT）
- 中：Price（现价）
- 右：24h%（涨绿跌红）

#### 列表态交互
- 点击 Row：
  - 若当前尺寸为 L/XL：进入详情态（K 线）
  - 若当前尺寸为 S/M：弹 Gate 引导层（见 6.3）
- 添加币种：
  - Header 右侧「+」按钮（或空列表主按钮）
  - 进入搜索选择（Symbol/Pair 搜索），选择后加入列表
- 删除币种：
  - Row 右键/更多菜单 → 删除（v1.0 必做）
- 排序：
  - v1.0 不做（避免复杂交互与状态）

### 6.2 详情态（K 线，仅 L/XL 可进入）
#### 展示区块
- 顶部信息：Pair + 当前价 + 24h%（红绿）
- 周期按钮：1m / 5m（默认高亮）/ 15m / 1h
- 主体：K 线（OHLC 蜡烛图）
- 返回：左上角返回箭头（固定一种，不做底部返回）

#### 详情态交互
- 切周期：点击周期按钮 → 重新拉取对应周期 OHLC（原型用跳帧表达即可）
- 退出详情：返回按钮 → 回列表态，并立即停止 OHLC 拉取（产品要求，验收项）

### 6.3 S/M Gate 引导层（强制）
触发：S/M 点击任一 Row  
展示：
- 文案：当前尺寸不支持 K 线，请切换到 L 或 XL 查看
- 按钮：
  - 切换到 L 查看（主按钮）
  - 切换到 XL 查看（次按钮）
  - 取消
行为：
- 点击切换到 L/XL：窗口切到对应尺寸，并自动进入该 Row 的详情态

## 7. 数据与请求策略（性能/限流红线）
### 7.1 列表数据（Ticker）
- 列表只拉：price、change24hPct（可选 change24hAbs）
- 多币种必须批量/合并请求：
  - 同 Provider 的 N 个币种 → 1 次批量请求
  - 不允许每行单独请求

### 7.2 详情数据（OHLC）
- 仅在详情态拉 OHLC
- 周期切换时才拉对应周期 OHLC
- 退出详情态后必须停止 OHLC 请求（不得后台静默拉取）

## 8. 数据展示口径（价格精度/格式）
- price 精度（默认规则）：
  - ≥1000：2 位小数
  - 1~999.99：2 位小数（如需更细可后续按币种精度表）
  - <1：最多 6 位小数
- change24hPct：2 位小数，如 -5.23%
- 颜色：涨=绿、跌=红、0=灰

## 9. 失败态（必须可观测 + 可修复）
### 9.1 列表态失败
- 断网（Offline）：
  - 仍显示上次成功快照（全表数值置灰）
  - 文案：更新于 X 分钟前
  - Footer：状态=断网；动作=重试
- 限流（RateLimit/429）：
  - 显示上次快照置灰 + 文案“已自动放慢刷新”
  - Footer：状态=限流；动作=切 Provider / 重试
- Key 缺失/失效：
  - 显示占位与引导文案
  - Footer：动作=修复 Key（进入 Key 向导）

### 9.2 详情态失败（OHLC 拉取失败）
- 若有上次 K 线缓存：置灰显示 + “更新于 X 分钟前”
- 若无缓存：显示错误占位页（不白屏）
- 提供：重试 / 修复 Key / 切 Provider / 返回列表

## 10. 原型 Frame 清单（设计可直接按此开画）
### 10.1 列表态
- MarketList-S（2 rows）
- MarketList-M（4 rows）
- MarketList-L（6 rows）
- MarketList-XL（10 rows）

### 10.2 Gate
- MarketGate-SM-To-LXL（引导层：切 L / 切 XL / 取消）

### 10.3 详情态（L/XL + 周期四帧）
- MarketDetail-L-1m
- MarketDetail-L-5m（默认入口）
- MarketDetail-L-15m
- MarketDetail-L-1h
- MarketDetail-XL-1m
- MarketDetail-XL-5m（默认入口）
- MarketDetail-XL-15m
- MarketDetail-XL-1h

### 10.4 失败态
- MarketList-Offline
- MarketList-RateLimit
- MarketList-KeyMissing
- MarketList-KeyInvalid
- MarketDetail-OHLC-Fail（通用一帧，内含“有缓存/无缓存”两种状态块）

### 10.5 Watchlist 管理
- Market-AddSymbol-Search（搜索选择）
- Market-EmptyState（空列表引导：添加币种）

## 11. 原型评审最低门槛（可点击验收）
1) S/M/L/XL 四档列表可切换（通过「…」菜单）  
2) S/M 点击 Row 必弹 Gate，并可一键切到 L/XL 且自动进入详情  
3) L/XL 点击 Row 直进详情，默认 5m，周期可切 1m/5m/15m/1h  
4) 详情可返回列表，返回后 OHLC 拉取停止（原型用“状态说明”标注；研发验收抓日志）  
5) 列表/详情失败态均显示上次快照置灰 + 更新时间，并可从 Footer 进入“修复 Key/重试/切 Provider”闭环

## 12. 初步验收标准（研发可测）
- 批量请求：10 个币种仅产生 1 次 Ticker 批量请求（抓包验证）
- Gate 可靠：S/M 点击 Row 100% 触发 Gate，不允许误入详情
- 默认周期：首次进入详情必为 5m，按钮高亮正确
- 停止拉取：退出详情后 3 秒内无任何 OHLC 网络请求
- 失败不空白：断网/限流时列表与详情均不空白，且显示“更新于 X 分钟前”

以上为“完整定稿版”，可直接用于出原型不需要再补分支口径。