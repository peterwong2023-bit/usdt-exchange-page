### 驴得稳的最终审查结论：

#### 1. 关于“日历/会议倒计时” (EventKit) —— 必须死磕权限引导
- **老驴洞察**：macOS 的权限弹窗一旦被用户点错“拒绝”，就得去系统设置里翻三层菜单才能找回来。
- **务实要求**：原型里必须做一个“权限预热页”。先在 App 里告诉用户“我们需要读取日历来显示会议”，用户点“确定”后再调系统弹窗。**别让用户在没心理准备的情况下做选择。**

#### 2. 关于“RSS 情报站” —— 严控刷新频率
- **老驴洞察**：RSS 源如果多，后台频繁拉取 XML 会导致系统卡顿。
- **务实要求**：默认刷新频率设为 **30 分钟**。别学行情软件搞秒级刷新，新闻没那么急。
- **简化级联**：如果用户开了 5 个 RSS 便签，底层必须合并为一个网络请求任务，按域名排队，严禁并发。

#### 3. 关于“通用行情卡” —— 统一数据单位
- **老驴洞察**：汇率、黄金、股票的单位全不一样（点位、美金、人民币）。
- **务实要求**：Body 区必须支持“自定义后缀”。用户填了黄金，单位得能选“元/克”或“美元/盎司”。**UI 框架要通用，但单位要能定制。**

#### 4. 关于“番茄钟” —— 必须有“静音模式”
- **老驴洞察**：在办公室里，番茄钟结束时的刺耳铃声是社交自杀。
- **务实要求**：默认结束动作是“便签闪烁”或“系统通知”，**声音默认关闭**。让用户自己去设置里开，别给用户找麻烦。

---

### 最终版 PRD 核心指标（老驴签字）：

| 维度 | 验收硬指标 |
| :--- | :--- |
| **性能** | 10 个便签同时运行，**内存占用不得超过 150MB**。 |
| **容错** | 任何 API 报错（401/429/500），Footer 必须显示**人类能看懂的中文提示**，严禁直接甩代码。 |
| **交互** | 所有“设置”操作必须在 **2 步内**触达（点齿轮 -> 改参数）。 |
| **离线** | 离线状态下，记事、番茄钟、日历（缓存）必须 **100% 可用**。 |

---

### [便签：老驴的最后叮嘱]
@驴码师 @驴架子 别跟我说“技术上实现不了”，我砍掉的都是难实现的。剩下的这些 P0，必须做到**“丝滑、不崩、不占资源”**。

@驴毕加索 原型里把“一键入会”和“权限预热”这两个细节做出来，这是体现产品“务实”的关键。

**审查完毕，可以开工。**